---
title: "ES-Merging: Biological MLLM Merging via Embedding Space Signals"
title_zh: ES-Merging：基于嵌入空间信号的生物多模态大语言模型合并
authors: "Wonbin Lee, Dongki Kim, Sung Ju Hwang"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14405v1"
tags: ["query:molrna"]
score: 8.0
evidence: 作为基础模型的生物多模态大语言模型
tldr: 针对生物多模态大语言模型（MLLM）在处理跨模态科学问题时受限于单一模态专业化的问题，本文提出了一种名为 ES-Merging 的表示感知合并框架。该方法摒弃了传统的参数空间启发式方法，转而利用嵌入空间信号来估计合并系数。通过设计探测输入并分析层级和元素级的嵌入响应，ES-Merging 实现了更精准的模态融合。实验证明，该方法在交互效应预测任务中表现优异，甚至超越了专门微调的模型。
selection_source: carryover_cache
motivation: 现有的模型合并方法多依赖于与输入无关的参数空间启发式规则，难以准确捕捉生物多模态模型中的模态专业化特征。
method: 通过探测输入获取各层嵌入响应，并从粗粒度的层级和细粒度的元素级两个维度联合估计互补的合并系数。
result: 在交互效应预测基准测试中，ES-Merging 的性能不仅超过了现有的合并技术，还优于针对特定任务微调的模型。
conclusion: 研究表明，嵌入空间信号是实现跨模态 MLLM 高效、稳健合并的一种具有原则性且有效的基础。
---

## 摘要
生物多模态大语言模型（MLLMs）已成为科学发现的强大基础模型。然而，现有模型通常专注于单一模态，限制了它们解决固有跨模态科学问题的能力。虽然模型合并是将不同模态组合成统一 MLLM 的有效方法，但现有方法依赖于与输入无关的参数空间启发式算法，无法准确捕捉模态专业化。为了克服这一局限性，我们提出了一种表示感知的合并框架，该框架通过嵌入空间信号来估计合并系数。我们首先设计了一个由不同模态 token 组成的探测输入，并将其通过每个专业化的 MLLM，以获得反映模态特定表示变化的逐层嵌入响应。然后，我们从嵌入空间中估计两个粒度的互补合并系数：来自粗粒度信号的逐层系数和来自细粒度信号的逐元素系数，并将两者结合以进行鲁棒的系数估计。在相互作用效应预测基准上的实验表明，我们的方法优于现有的合并方法，甚至超过了特定任务的微调模型，证明了嵌入空间信号为跨模态 MLLM 合并提供了原则性且有效的基础。

## Abstract
Biological multimodal large language models (MLLMs) have emerged as powerful foundation models for scientific discovery. However, existing models are specialized to a single modality, limiting their ability to solve inherently cross-modal scientific problems. While model merging is an efficient method to combine the different modalities into a unified MLLM, existing methods rely on input-agnostic parameter space heuristics that fail to faithfully capture modality specialization. To overcome this limitation, we propose a representation-aware merging framework that estimates merging coefficients from embedding space signals. We first design a probe input that consists of different modality tokens and forward it through each specialized MLLM to obtain layer-wise embedding responses that reflect modality-specific representation changes. We then estimate complementary merging coefficients at two granularities from the embedding space: layer-wise coefficients from coarse-grained signals and element-wise coefficients from fine-grained signals, which are jointly combined for robust coefficient estimation. Experiments on interactive effect prediction benchmarks show that our method outperforms existing merging methods and even surpasses task-specific fine-tuned models, establishing that embedding space signals provide a principled and effective foundation for cross-modal MLLM merging.

---

## 论文详细总结（自动生成）

### ES-Merging: 基于嵌入空间信号的生物多模态大语言模型合并论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
现有的生物多模态大语言模型（MLLM）通常只针对单一模态（如分子、蛋白质或细胞）进行优化，难以处理复杂的跨模态科学问题（如药物与蛋白质的相互作用）。虽然可以通过“模型合并”技术将多个专家模型组合，但现有的合并方法（如 TIES-Merging）主要依赖于**参数空间**的启发式规则（如权重的量级、符号），这些规则与输入无关，无法准确捕捉模型对特定模态的理解深度。本文提出了 **ES-Merging**，旨在通过**嵌入空间（Embedding Space）**的信号来引导模型合并，使合并过程能够感知输入并保留各模态的专业知识。

#### 2. 方法论：核心思想与关键技术
ES-Merging 的核心思想是：一个专家模型对特定模态的专业化程度，可以通过其隐藏层表示（Embedding）相对于基础模型的偏移程度来衡量。

*   **探测输入（Probe Input）：** 设计一个包含分子、蛋白质和细胞 token 的混合 Prompt，将其输入到基础 LLM 和各个专业化 MLLM 中。
*   **双粒度系数估计：**
    *   **层级全局系数（Layer-wise Global Coefficient, $\alpha$）：** 使用**切片瓦瑟斯坦距离（Sliced Wasserstein Distance, SWD）**计算每一层嵌入分布的变化。如果某一层在处理特定模态时产生了显著的分布偏移，则赋予该层更高的合并权重。
    *   **元素级局部系数（Element-wise Local Coefficient, $\beta$）：** 通过计算嵌入空间距离对参数的梯度，识别出对模态表示偏移最敏感的具体参数元素，实现精细化的权重分配。
*   **系数集成：** 将层级系数与元素级系数相乘并归一化，得到最终的合并系数 $\lambda$，用于加权融合各模型的 LoRA 参数。

#### 3. 实验设计
*   **数据集与场景：**
    *   **实例变化交互预测：** 分子-蛋白质相互作用（BindingDB, BioSNAP, Human）、分子-细胞相互作用（DrugComb, GDSC2）。
    *   **目标固定功能预测：** CYP 酶抑制预测（5 个子任务）、CYP 底物特异性预测（3 个子任务）。
*   **Benchmark 与对比方法：**
    *   **基准模型：** LLaMA-3.1-8B-Instruct 及其衍生专家模型（Mol-LLaMA, Prot2Text-V2, Cell-o1）。
    *   **对比合并方法：** Average Merging, TIES-Merging, EMR-Merging, AdaMerging, PCB-Merging, Consensus Merging, LS-Merge。
    *   **其他对比：** 针对特定任务进行微调的模型（Avg. Merging + FT）。

#### 4. 资源与算力
*   **模型配置：** 使用 LLaMA-3.1-8B 作为骨干网络，LoRA 配置为 $r=8, \alpha=32$。
*   **算力说明：** 论文**未明确列出具体的 GPU 型号和数量**。但强调了计算效率：ES-Merging 仅需一次梯度计算，其总浮点运算量（FLOPs）比 AdaMerging 低 3.4 倍，比特定任务微调低 6.1 倍。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 10 多个生物科学数据集，进行了准确率（Acc）和 Macro-F1 值的全面评估。
*   **消融实验：** 针对“仅使用层级系数”、“仅使用元素级系数”以及“两者结合”进行了对比，验证了双粒度设计的必要性。
*   **定性分析：** 提供了案例研究（Case Study），展示了合并模型在推理过程中的生物学解释能力。
*   **评价：** 实验设计较为充分，涵盖了从基础交互到复杂功能的多种生物学场景，对比基准涵盖了近年主流的模型合并技术，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能卓越：** ES-Merging 在所有跨模态任务中均优于现有的模型合并方法。
*   **超越微调：** 在某些任务上，ES-Merging 的表现甚至超过了经过特定任务微调的模型，且保留了更好的推理逻辑。
*   **嵌入信号的有效性：** 实验证明，嵌入空间的分布差异确实能比参数统计量更有效地反映模态专业化特征。
*   **多粒度互补：** 层级系数捕捉全局重要性，元素级系数捕捉局部敏感性，两者结合能实现最稳健的合并。

#### 7. 优点（亮点）
*   **表示感知（Representation-aware）：** 突破了传统合并方法“输入盲（Input-blind）”的局限，利用模型运行时的动态信号。
*   **高效性：** 无需迭代训练或复杂的测试时自适应，计算成本极低。
*   **可解释性：** 通过嵌入空间的可视化和推理案例，证明了合并模型能够有机地整合不同模态的知识，而非简单的权重堆叠。

#### 8. 不足与局限
*   **领域局限：** 实验仅集中在生物化学领域，尚未在图像、视频、音频等通用多模态领域验证其普适性。
*   **单模态性能保留：** 论文主要关注跨模态融合任务，对于合并后模型在原始单模态任务（如纯蛋白质功能预测）上的性能保留情况探讨较少。
*   **探测输入的依赖：** 合并效果可能受到探测输入（Probe Input）样本选择的影响，论文未深入探讨样本多样性对系数稳定性的影响。

（完）
