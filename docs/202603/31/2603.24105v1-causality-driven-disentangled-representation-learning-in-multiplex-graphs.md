---
title: Causality-Driven Disentangled Representation Learning in Multiplex Graphs
title_zh: 多层图中因果驱动的解耦表示学习
authors: "Saba Nasiri, Selin Aviyente, Dorina Thanou"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24105v1"
tags: ["query:molrna"]
score: 8.0
evidence: 多层图中的解耦表示学习
tldr: 针对多层图中共享与层特定信息纠缠导致泛化性差的问题，本文提出基于因果推理的解耦表示学习框架CaDeM。该框架通过自监督方式，结合嵌入对齐、私有信号捕获及因果后门调整技术，实现了公共与私有信息的有效分离。实验证明，该方法在多个数据集上显著提升了多层图表示学习的性能与可解释性。
selection_source: fresh_fetch
motivation: 多层图中共享信息与层特定信息的相互纠缠限制了模型在处理复杂关系网络时的泛化能力和可解释性。
method: 提出CaDeM框架，利用因果后门调整和自监督学习来对齐共享嵌入并分离层特定的私有嵌入。
result: 在合成和真实世界数据集上的实验一致表明，该方法在多层图表示学习任务中优于现有的主流基准模型。
conclusion: 通过因果驱动的解耦机制，可以有效提取多层图中的全局与局部特征，增强模型的鲁棒性与解释力。
---

## 摘要
从多层图（即节点通过多种关系类型交互的多层网络）中学习表示具有挑战性，因为共享（公共）信息和层特定（私有）信息相互纠缠，这限制了泛化能力和可解释性。在这项工作中，我们引入了一个基于因果推理的框架，以自监督的方式解耦公共和私有组件。CaDeM 联合执行以下操作：(i) 对齐跨层的共享嵌入，(ii) 强制私有嵌入捕获层特定的信号，以及 (iii) 应用后门调整以确保公共嵌入仅捕获全局信息，同时与私有表示分离。在合成数据集和真实世界数据集上的实验表明，与现有基准相比，该方法具有持续的改进，突显了我们方法在鲁棒且可解释的多层图表示学习方面的有效性。

## Abstract
Learning representations from multiplex graphs, i.e., multi-layer networks where nodes interact through multiple relation types, is challenging due to the entanglement of shared (common) and layer-specific (private) information, which limits generalization and interpretability. In this work, we introduce a causal inference-based framework that disentangles common and private components in a self-supervised manner. CaDeM jointly (i) aligns shared embeddings across layers, (ii) enforces private embeddings to capture layer-specific signals, and (iii) applies backdoor adjustment to ensure that the common embeddings capture only global information while being separated from the private representations. Experiments on synthetic and real-world datasets demonstrate consistent improvements over existing baselines, highlighting the effectiveness of our approach for robust and interpretable multiplex graph representation learning.

---

## 论文详细总结（自动生成）

以下是对论文《Causality-Driven Disentangled Representation Learning in Multiplex Graphs》（多层图中因果驱动的解耦表示学习）的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多层图（Multiplex Graphs，即同一组节点在不同层具有不同关系类型）中，节点的表示通常由**共享（公共）信息**和**层特定（私有）信息**共同决定。
*   **研究动机**：现有方法往往将这两类信息混合在一个嵌入空间中，导致全局模式被层特定噪声干扰，或层特定特征被过度平滑。这种“信息纠缠”限制了模型的泛化能力、鲁棒性和可解释性。
*   **目标**：通过因果推理手段，显式地将公共嵌入和私有嵌入解耦，使每一部分仅捕获其预期的特征。

### 2. 方法论（CaDeM 框架）
CaDeM 建立在图卷积网络（GCN）骨干之上，通过三个协同目标实现解耦：
*   **核心思想**：将私有嵌入视为预测任务的“因果特征”，将公共嵌入视为可能诱发虚假相关性的“混杂因子（Confounder）”，利用因果干预中的**后门调整（Backdoor Adjustment）**来消除混杂影响。
*   **关键技术细节**：
    1.  **匹配损失 ($L_{matching}$)**：利用广义普鲁克分析（GPA）对齐各层的公共嵌入，强制其映射到共享的潜在空间，捕获跨层不变的全局结构。
    2.  **自监督损失 ($L_{self-supervised}$)**：训练私有嵌入去预测其所属的层索引（Layer Index）。这确保了私有嵌入能最大化捕获该层特有的、足以区分于其他层的信号。
    3.  **因果损失 ($L_{causal}$)**：这是本文的核心创新。通过**混杂分层（Confounder Stratification）**和**重新配对**，将某层的私有嵌入与另一随机层的公共嵌入组合进行预测。这种策略模拟了因果后门调整，迫使预测器仅依赖私有特征，从而切断公共特征与层特定目标之间的虚假关联。
    4.  **图增强**：通过节点采样和注入高斯噪声生成增强图，以支持更稳健的因果损失估计。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据集 (Syn1–Syn4)**：分别模拟了结构差异、局部扰动、频谱变化和非线性动力学差异，用于验证解耦的精确度。
    *   **真实世界数据集**：包括引用网络（DBLP, ACM）、电影网络（IMDB, Freebase）以及人类连接组计划（HCP）的脑功能磁共振成像（fMRI）数据。
*   **Benchmark 与对比方法**：
    *   **单视图方法**：DeepWalk, Node2Vec, VGAE, DGI, MVGRL, GraphMAE。
    *   **多层图方法**：MNE, HDMI, MCGC, DMG。
    *   **图级别方法**：graph2vec。
*   **评估任务**：节点分类、节点聚类、图分类。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 Adam 优化器，嵌入维度固定为 8，权重衰减为 $10^{-4}$，并给出了各数据集的具体超参数设置（如学习率、损失权重等）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个合成场景和 5 个真实数据集，总计 9 个实验环境。
*   **充分性评价**：
    *   **多维度验证**：不仅对比了 F1 分数，还进行了消融实验（验证三个损失项的必要性）、敏感性分析、t-SNE 可视化以及脑网络的可解释性分析。
    *   **客观性**：采用了嵌套分层 K 折交叉验证（Nested Stratified K-fold CV），确保了模型选择和性能评估的无偏性。
    *   **理论支撑**：提供了关于解耦保证的数学证明，增强了实验结果的说服力。

### 6. 主要结论与发现
*   **性能领先**：CaDeM 在所有节点级和图级任务中均一致优于现有基准模型，尤其在处理复杂动力学系统（Syn4）和脑网络（HCP）时表现突出。
*   **成功解耦**：合成实验证明，公共嵌入确实只捕获共享结构，而私有嵌入能精准捕获层特定的频谱或动力学特征。
*   **神经科学发现**：在 HCP 数据中，CaDeM 提取的特征与人类大脑在静息态、情绪处理和工作记忆任务中的已知功能分区高度吻合，证明了其生物学意义上的可解释性。

### 7. 优点与亮点
*   **因果视角**：首次将因果后门调整引入多层图解耦表示学习，提供了比传统正交约束或对比学习更严谨的解耦机制。
*   **理论与实践结合**：不仅有直观的框架设计，还提供了详尽的概率论证明（如 Proposition 1 & 2, Theorem 1）。
*   **高可解释性**：通过解耦，模型能够清晰地展示哪些特征是全局共有的，哪些是特定关系独有的，这对于医疗、社交分析等领域至关重要。

### 8. 不足与局限
*   **节点对齐假设**：模型假设多层图中的节点身份是完全对齐的，无法直接处理节点集部分重叠或完全不同的异构多层图。
*   **层索引代理**：自监督任务依赖于“层索引”作为预测目标，在层数极多或层间差异极细微的情况下，该目标的有效性可能下降。
*   **计算开销**：虽然复杂度分析显示其呈线性增长，但因果损失中的重新配对（Stratified Pairing）在处理超大规模多层图时可能会增加内存负担。

（完）
