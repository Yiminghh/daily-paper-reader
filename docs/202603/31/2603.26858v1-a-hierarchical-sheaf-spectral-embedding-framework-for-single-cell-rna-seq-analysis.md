---
title: A Hierarchical Sheaf Spectral Embedding Framework for Single-Cell RNA-seq Analysis
title_zh: 一种用于单细胞 RNA 测序分析的层级化层谱嵌入框架
authors: "Xiang Xiang Wang, Guo-Wei We"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26858v1"
tags: ["query:molrna"]
score: 9.0
evidence: 用于RNA-seq分析的分层层束谱嵌入
tldr: 本研究针对单细胞RNA-seq数据分析中捕捉多尺度异质局部结构的需求，提出了层级层谱嵌入（HSSE）框架。该框架利用持久层拉普拉斯分析，在不同分辨率的局部邻域内构建数据驱动的细胞层，并提取总结局部关系演化的谱统计量作为细胞特征。HSSE无需额外模型训练即可生成统一的特征向量，在12个基准数据集上展现了卓越的性能，为单细胞数据提供了一种稳健且可解释的表示学习方法。
selection_source: fresh_fetch
motivation: 单细胞RNA-seq分析需要一种既能捕捉多尺度异质局部结构，又具备稳定性和可解释性的特征表示方法。
method: 提出HSSE框架，通过在多分辨率局部邻域上构建细胞层，并利用持久层拉普拉斯算子提取跨尺度的谱统计特征。
result: 在12个基准数据集的分类任务中，HSSE相比现有的多尺度和经典嵌入方法取得了竞争性或更优的性能。
conclusion: 层谱表示法为单细胞RNA-seq数据提供了一种无需额外训练、稳健且具有高度解释性的表示学习新方案。
---

## 摘要
单细胞 RNA 测序数据分析通常需要能够捕捉跨多个尺度的异质局部结构，同时保持稳定性和可解释性的表示。在这项工作中，我们提出了一个层级化层谱嵌入（HSSE）框架，该框架基于持续层拉普拉斯（persistent sheaf Laplacian）分析构建具有信息量的细胞级特征。从依赖于尺度的低维嵌入出发，我们在多个分辨率下定义了以细胞为中心的局部邻域。对于每个局部邻域，我们构建了一个数据驱动的细胞层（cellular sheaf），用于编码细胞间的局部关系。然后，我们在采样的过滤区间上计算持续层拉普拉斯算子，并提取总结局部关系结构跨尺度演变的谱统计量。这些谱描述符被聚合为每个细胞的统一特征向量，并可直接用于下游学习任务，无需额外的模型训练。我们在涵盖多种生物系统和数据规模的 12 个基准单细胞 RNA 测序数据集上评估了 HSSE。在一致的分类协议下，与现有的多尺度和经典的基于嵌入的方法相比，HSSE 在多个评估指标上实现了具有竞争力或更优的性能。结果表明，层谱表示为单细胞 RNA 测序数据表示学习提供了一种稳健且可解释的方法。

## Abstract
Single-cell RNA-seq data analysis typically requires representations that capture heterogeneous local structure across multiple scales while remaining stable and interpretable. In this work, we propose a hierarchical sheaf spectral embedding (HSSE) framework that constructs informative cell-level features based on persistent sheaf Laplacian analysis. Starting from scale-dependent low-dimensional embeddings, we define cell-centered local neighborhoods at multiple resolutions. For each local neighborhood, we construct a data-driven cellular sheaf that encodes local relationships among cells. We then compute persistent sheaf Laplacians over sampled filtration intervals and extract spectral statistics that summarize the evolution of local relational structure across scales. These spectral descriptors are aggregated into a unified feature vector for each cell and can be directly used in downstream learning tasks without additional model training. We evaluate HSSE on twelve benchmark single-cell RNA-seq datasets covering diverse biological systems and data scales. Under a consistent classification protocol, HSSE achieves competitive or improved performance compared with existing multiscale and classical embedding-based methods across multiple evaluation metrics. The results demonstrate that sheaf spectral representations provide a robust and interpretable approach for single-cell RNA-seq data representation learning.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：A Hierarchical Sheaf Spectral Embedding Framework for Single-Cell RNA-seq Analysis

#### 1. 核心问题与整体含义（研究动机和背景）
单细胞 RNA 测序（scRNA-seq）数据具有**高维度、高噪声和复杂的多尺度层级结构**。现有的分析挑战在于如何构建既能捕捉局部异质性，又能在不同生物尺度下保持稳健性和可解释性的特征表示。
*   **局限性：** 传统的降维方法（如 PCA, t-SNE, UMAP）通常依赖单一的邻域分辨率，难以全面刻画细胞间的层级关系。
*   **动机：** 引入数学中的**层论（Sheaf Theory）**和**持久拉普拉斯（Persistent Laplacian）**，通过构建局部“细胞层”来编码细胞间的复杂关系，从而提取能够反映几何与拓扑演化特征的谱描述符。

#### 2. 论文提出的方法论：HSSE 框架
HSSE（层级化层谱嵌入）框架通过以下五个关键步骤实现特征提取：
1.  **多尺度低维表示：** 首先利用降维技术（如 UMAP）在不同参数下生成一系列依赖尺度的低维嵌入，捕捉从微观局部到宏观组织的结构。
2.  **以细胞为中心的局部邻域：** 对于每个细胞，在不同尺度下构建其 $k$ 近邻（k-NN）图，形成局部单纯复形（Simplicial Complexes）。
3.  **构建细胞层（Cellular Sheaf）：** 在局部复形上定义“层”结构。核心在于**限制映射（Restriction Maps）**的设计，它结合了：
    *   **几何核：** 基于欧氏距离的高斯核，强化近邻关系。
    *   **标签调制：** 区分中心细胞与邻居细胞的身份，增强局部对比度。
4.  **计算持久层拉普拉斯（PSL）谱：** 在过滤（Filtration）过程中，计算不同区间内的持久层拉普拉斯算子，提取其特征值。
5.  **特征聚合：** 提取特征值的统计量（均值、标准差、最大/最小值等），拼接成统一的细胞级特征向量，直接输入下游分类器（如 GBDT）。

#### 3. 实验设计
*   **数据集：** 使用了 **12 个基准单细胞数据集**（包括 Deng, Baron, Chu, Villani 等），涵盖人类和小鼠的免疫细胞、神经元、胰腺细胞等多种生物系统。
*   **Benchmark 与对比方法：**
    *   **多尺度方法：** MDG（多尺度微分几何学习）。
    *   **经典降维方法：** PCA, UMAP, t-SNE, NMF。
*   **评估协议：** 统一使用梯度提升决策树（GBDT）作为分类器，采用五折交叉验证，评估指标包括 Macro-F1、Macro-Recall 和 Macro-AUC。

#### 4. 资源与算力
*   **算力说明：** 论文中**未明确说明**具体的硬件配置（如 GPU 型号、显存）或具体的训练/推理时长。
*   **软件实现：** 提到了使用专门开发的持久拓扑拉普拉斯软件工具包（PETLS）进行大规模计算，暗示了该方法在算法实现上考虑了计算效率。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 12 个不同规模和复杂度的真实数据集进行了测试，样本量从几百到几千个细胞不等。
*   **消融与敏感性分析：** 
    *   研究了**邻域大小（K）**的影响（从 1 到 10 组邻域配置）。
    *   研究了**尺度配置数量（S）**的影响（从 1 到 5 个尺度）。
    *   对比了 0 维与 1 维同调特征的贡献。
*   **客观性：** 实验采用了固定的分类器超参数，避免了针对特定数据集的过度调优，对比过程较为公平、客观。

#### 6. 主要结论与发现
*   **性能优越：** HSSE 在大多数数据集上的 Macro-F1 和 Macro-Recall 均优于 MDG 和经典降维方法，平均性能提升约 2%-4%。
*   **多尺度必要性：** 实验证明，聚合多个邻域大小和嵌入尺度能显著提升分类准确率，单一尺度往往无法捕捉完整的生物异质性。
*   **拓扑贡献：** 0 维特征（连通性）在分类中起主导作用，而 1 维特征（环状结构）在特定复杂数据集中能提供补充信息。
*   **稳健性：** 该框架对超参数（如尺度数量）的微小变化不敏感，表现出良好的泛化能力。

#### 7. 优点
*   **理论严谨：** 将抽象的层论转化为可计算的谱特征，为单细胞分析提供了坚实的数学基础。
*   **无需训练：** 作为一种特征工程方法，它不需要像深度学习那样进行大规模端到端训练，具有较好的可解释性。
*   **局部敏感：** 通过以细胞为中心的层构建，能够比全局拓扑方法更精细地刻画局部细胞状态的微小差异。

#### 8. 不足与局限
*   **计算复杂度：** 尽管有专门软件支持，但为每个细胞构建多个局部复形并计算拉普拉斯谱，其计算开销仍可能随细胞数量和邻域大小呈非线性增长。
*   **极端数据表现：** 在样本量极小（如 GSE45719）或极大型且高度异质的数据集上，HSSE 的优势相对减弱，说明其性能受限于局部邻域估计的准确性。
*   **应用范围：** 目前主要验证了分类任务，在细胞轨迹推断、空间转录组等更复杂的下游任务中的表现尚待进一步探索。

（完）
