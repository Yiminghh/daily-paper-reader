---
title: "ConfHit: Conformal Generative Design with Oracle Free Guarantees"
title_zh: ConfHit：具有无 Oracle 保证的符合性生成式设计
authors: "Siddhartha Laghuvarapu, Ying Jin, Jimeng Sun"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07371v1"
tags: ["query:molrna"]
score: 8.0
evidence: 具有有效性保证的药物发现符合性生成设计
tldr: 针对科学发现中生成模型缺乏可靠属性保证的问题，本文提出 ConfHit 框架。该框架在无需实验真值且存在分布偏移的情况下，利用加权保形预测技术，为生成的候选集合提供统计学上的“命中”保证。ConfHit 能够认证生成批次中是否包含有效样本，并能精简候选集，在药物分子设计等任务中实现了可靠的性能保障。
selection_source: carryover_cache
motivation: 现有的生成模型在科学发现中面临预算限制、缺乏实时实验验证以及分布偏移的挑战，难以保证生成结果的有效性。
method: 提出一种基于加权交换性的保形预测框架，通过密度比加权的 p 值和嵌套测试程序，在无需实验真值的情况下实现对多样本批次的命中认证与集合精简。
result: 在多种分子生成设计任务中，ConfHit 在不同置信水平下均能提供有效的覆盖率保证，并保持了认证候选集的紧凑性。
conclusion: ConfHit 为生成式科学设计提供了一个原则性强且可靠的统计框架，有效解决了分布偏移下的生成质量评估难题。
---

## 摘要
深度生成模型在科学发现中的成功，不仅需要生成新颖候选样本的能力，还需要可靠的保证，确保这些候选样本确实满足所需的属性。最近的符合性预测（conformal prediction）方法为提供此类保证提供了一条路径，但其在药物发现生成建模中的应用受到预算限制、缺乏 Oracle 访问权限以及分布偏移的限制。为此，我们引入了 ConfHit，这是一个在这些条件下提供有效性保证的无分布框架。ConfHit 将两个核心问题形式化：(i) 认证：能否保证生成的批次在用户指定的置信水平下包含至少一个“命中”（hit）；(ii) 设计：能否在不削弱该保证的情况下，将生成结果精炼为一个紧凑的集合。ConfHit 利用历史样本与生成样本之间的加权可交换性，消除了对实验 Oracle 的需求；构建了多样本密度比加权符合性 p 值，以量化命中的统计置信度；并提出了一种嵌套测试程序，在保持统计保证的同时，对多个生成样本的候选集进行认证和精炼。在代表性的生成式分子设计任务和广泛的方法中，ConfHit 在保持紧凑认证集的同时，始终在多个置信水平下提供有效的覆盖保证，为生成建模建立了一个有原则且可靠的框架。

## Abstract
The success of deep generative models in scientific discovery requires not only the ability to generate novel candidates but also reliable guarantees that these candidates indeed satisfy desired properties. Recent conformal-prediction methods offer a path to such guarantees, but its application to generative modeling in drug discovery is limited by budget constraints, lack of oracle access, and distribution shift. To this end, we introduce ConfHit, a distribution-free framework that provides validity guarantees under these conditions. ConfHit formalizes two central questions: (i) Certification: whether a generated batch can be guaranteed to contain at least one hit with a user-specified confidence level, and (ii) Design: whether the generation can be refined to a compact set without weakening this guarantee. ConfHit leverages weighted exchangeability between historical and generated samples to eliminate the need for an experimental oracle, constructs multiple-sample density-ratio weighted conformal p-value to quantify statistical confidence in hits, and proposes a nested testing procedure to certify and refine candidate sets of multiple generated samples while maintaining statistical guarantees. Across representative generative molecule design tasks and a broad range of methods, ConfHit consistently delivers valid coverage guarantees at multiple confidence levels while maintaining compact certified sets, establishing a principled and reliable framework for generative modeling.

---

## 论文详细总结（自动生成）

以下是对论文《ConfHit: Conformal Generative Design with Oracle Free Guarantees》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在药物发现等科学领域，深度生成模型虽然能产生大量候选分子，但其实际应用受限于**可靠性保证**。现有的符合性预测（Conformal Prediction, CP）方法虽能提供统计保证，但在实际场景中面临三大挑战：
*   **Oracle 访问受限**：现有方法通常假设可以立即调用“Oracle”（如昂贵的湿实验）来验证新生成的样本，这在资源受限的初期筛选中是不现实的。
*   **分布偏移（Distribution Shift）**：生成模型产生的样本分布往往与历史实验数据（校准集）不一致，违反了 CP 的基本可交换性假设。
*   **预算与效率**：如何在有限的生成预算内，既保证批次中至少包含一个有效样本（Hit），又能尽可能精简候选集以降低后续实验成本。

**ConfHit** 的核心意义在于：它是一个模型无关的框架，能够在**无需实时实验验证**且**存在分布偏移**的情况下，为生成结果提供严格的统计有效性保证。

### 2. 方法论：核心思想与关键技术
ConfHit 将问题拆解为“认证（Certification）”和“设计（Design）”两个阶段：

*   **核心思想**：利用历史标注数据（校准集）和未标注的生成样本，通过**密度比加权（Density-ratio weighting）**来修正分布偏移，从而在生成样本上构建有效的 p 值。
*   **关键技术细节**：
    1.  **密度比估计**：计算生成分布 $Q$ 与历史分布 $P$ 之间的似然比 $w(x) = dQ/dP$。论文采用核密度估计（KDE）在属性预测模型的倒数第二层特征空间进行估计。
    2.  **联合加权符合性 p 值**：针对一个包含 $N$ 个样本的批次，构建一个联合 p 值，用于检验“该批次中没有任何命中”的零假设。
    3.  **嵌套测试程序（Nested Testing）**：为了实现“设计”目标（即精简集合），ConfHit 对嵌套的候选子集序列进行测试。算法会找到满足置信水平 $1-\alpha$ 的**最小**子集 $\hat{N}$。
    4.  **单调性处理**：通过对 p 值序列进行单调化处理，确保在不进行多重假设检验校正的情况下，依然能控制有限样本的错误率。

### 3. 实验设计
论文在两个代表性的分子生成任务上进行了验证：
*   **任务 1：约束分子优化（CMO）**。目标是优化分子的 DRD2 受体结合活性和 QED（药物相似性）。
    *   **模型**：HGraph2Graph（VAE 架构）、Self-Edit（Transformer 架构）。
*   **任务 2：基于结构的药物发现（SBDD）**。目标是根据蛋白质口袋生成高亲和力配体。
    *   **模型**：TargetDiff（扩散模型）、DecompDiff、MolCraft（贝叶斯流网络）。
*   **Benchmark 与对比方法**：
    *   **对比基准**：Bonferroni 校正（过于保守）、仅认证（不精简集合）、启发式概率校准（无统计保证）、基于 Oracle 的现有 CP 方法（作为不可达到的上限）。
    *   **评估指标**：实际错误率（Realized Error Rate）、认证比例（Power）、平均集合大小、空集比例。

### 4. 资源与算力
论文明确列出了实验环境与开销：
*   **硬件**：单张 **NVIDIA A100 GPU**。
*   **耗时**：
    *   属性预测器训练：约 20 分钟。
    *   生成模型训练：HGraph 约 10 小时，Self-Edit 约 48 小时，SBDD 相关模型约 24 小时。
    *   **ConfHit 推理**：处理 800 个测试样本约需 30 分钟（平均每个样本约 2.25 秒），显示出较高的实用效率。

### 5. 实验数量与充分性
实验设计非常充分且客观：
*   **多模型覆盖**：涵盖了 VAE、Transformer、扩散模型和 BFN 等主流生成架构，证明了方法的**模型无关性**。
*   **消融与鲁棒性实验**：
    *   测试了 5 种不同的评分统计量（Max, Mean, Rank-sum 等）。
    *   进行了**分布偏移校正的消融**，证明不进行加权会导致明显的覆盖率失效。
    *   **预测器质量影响**：即使使用带有噪声或错误的预测模型，ConfHit 依然能保持错误率控制（仅损失效能/Power）。
    *   **敏感性分析**：针对密度比估计误差进行了鲁棒性诊断。

### 6. 主要结论与发现
*   **有效性**：ConfHit 在所有任务和模型中均能严格将错误率控制在预设的 $\alpha$ 水平之下。
*   **精简性**：相比于 Bonferroni 等传统方法，ConfHit 能显著减小认证集合的大小（通常能减少 30%-50% 的样本量），且空集率更低。
*   **鲁棒性**：该方法对属性预测器的准确性不敏感，只要密度比估计相对合理，就能维持统计有效性。
*   **实用性**：在固定实验预算分配任务中，ConfHit 提供的置信度可以作为有效的启发式工具来优化资源分配。

### 7. 优点
*   **无需 Oracle**：解决了 CP 在生成式科学设计中最大的落地障碍。
*   **理论完备**：提供了有限样本下的数学证明，确保了在分布偏移下的有效性。
*   **灵活性高**：可以插入任何现有的生成模型和属性预测器，无需重新训练生成模型。
*   **嵌套测试设计**：巧妙地解决了“既要保证命中，又要减少实验量”的矛盾。

### 8. 不足与局限
*   **密度比估计依赖**：在高维空间或校准数据极度匮乏时，密度比估计（如 KDE）可能变得不稳定，虽然论文提供了诊断工具，但这仍是潜在的失效点。
*   **计算 Oracle 的局限**：实验验证使用的是计算模拟 Oracle（如 AutoDock Vina），虽然这是领域惯例，但与真实的湿实验环境仍可能存在差距。
*   **大分子扩展性**：目前主要针对小分子，对于蛋白质等具有更复杂结构空间的生物大分子，其特征提取和密度估计的有效性尚待进一步验证。

（完）
