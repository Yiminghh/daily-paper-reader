---
title: Multi-RF Fusion with Multi-GNN Blending for Molecular Property Prediction
title_zh: 用于分子性质预测的多随机森林融合与多图神经网络混合方法
authors: Zacharie Bugaud
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20724v1"
tags: ["query:molrna"]
score: 9.0
evidence: 用于分子性质预测的图神经网络融合
tldr: "本研究提出Multi-RF Fusion方法，在ogbg-molhiv分子性质预测任务中取得榜单第一。该方法核心是将12个基于4263维拼接分子指纹训练的随机森林模型进行秩平均集成，并以12%的权重融合多种子深度集成GNN预测。通过优化随机森林特征采样比例及预先平均GNN种子预测，显著提升了AUC并大幅降低了结果方差，且无需任何外部数据或预训练。"
selection_source: fresh_fetch
motivation: 旨在通过融合传统随机森林模型与深度学习GNN模型，解决分子性质预测中的性能瓶颈及模型方差问题。
method: 利用拼接的多种分子指纹训练12个随机森林模型进行秩平均集成，并与多种子GNN预测结果进行加权融合。
result: 在ogbg-molhiv数据集上实现0.8476的ROC-AUC，超越HyperFusion位列OGB榜单首位，且标准差降至0.0002。
conclusion: 通过合理的模型融合策略和超参数优化，传统机器学习与深度学习的结合能在分子表征任务中展现出极强的竞争力。
---

## 摘要
Multi-RF Fusion 在 ogbg-molhiv 数据集（10 个随机种子）上实现了 0.8476 +/- 0.0002 的测试 ROC-AUC，在 OGB 排行榜上排名第一，领先于 HyperFusion (0.8475 +/- 0.0003)。该方法的核心是对 12 个随机森林模型进行秩平均集成，这些模型在拼接的分子指纹（FCFP、ECFP、MACCS、原子对——共 4,263 维）上进行训练，并以 12% 的权重与深度集成的 GNN 预测结果进行混合。两个发现推动了这一结果：(1) 在该骨架划分（scaffold split）上，将 max_features 设置为 0.20 而非默认的 sqrt(d) 带来了 +0.008 的 AUC 提升；(2) 在与随机森林混合之前，先对 10 个种子的 GNN 预测结果取平均，完全消除了 GNN 的种子方差，使最终的标准差从 0.0008 降至 0.0002。该方法未使用外部数据或预训练。

## Abstract
Multi-RF Fusion achieves a test ROC-AUC of 0.8476 +/- 0.0002 on ogbg-molhiv (10 seeds), placing #1 on the OGB leaderboard ahead of HyperFusion (0.8475 +/- 0.0003). The core of the method is a rank-averaged ensemble of 12 Random Forest models trained on concatenated molecular fingerprints (FCFP, ECFP, MACCS, atom pairs -- 4,263 dimensions total), blended with deep-ensembled GNN predictions at 12% weight. Two findings drive the result: (1) setting max_features to 0.20 instead of the default sqrt(d) gives a +0.008 AUC gain on this scaffold split, and (2) averaging GNN predictions across 10 seeds before blending with the RF eliminates GNN seed variance entirely, dropping the final standard deviation from 0.0008 to 0.0002. No external data or pre-training is used.

---

## 论文详细总结（自动生成）

以下是对论文《Multi-RF Fusion with Multi-GNN Blending for Molecular Property Prediction》的结构化总结：

### 1. 核心问题与整体含义
该研究针对分子性质预测任务（特别是 **ogbg-molhiv** 数据集）中存在的两个核心挑战：
*   **骨架泛化（Scaffold Generalization）：** 测试集分子与训练集具有完全不同的化学骨架，导致许多深度学习模型泛化能力差。
*   **高方差问题：** 现有的图神经网络（GNN）在不同随机种子下的表现波动剧烈（AUC 波动可达 0.04），导致结果不可靠。
论文的整体含义在于证明：通过**传统机器学习（随机森林）与深度学习（GNN）的深度融合**，并配合精细的超参数调整，可以在不使用外部数据或预训练的情况下，超越纯深度学习模型，取得 OGB 排行榜第一的成绩。

### 2. 方法论
核心思想是构建一个以随机森林（RF）为主、GNN 为辅的集成预测系统。
*   **特征工程：** 拼接了 4,263 维的分子指纹，包括 FCFP（药效团信息）、ECFP（结构邻域）、MACCS 密钥和原子对特征。
*   **Multi-RF 融合：** 训练 12 个随机森林模型（每个 20,000 棵树）。关键技术细节是将 `max_features` 从默认的 $\sqrt{d}$ 提高到 **0.20**，使每棵树能同时捕捉到药效团和结构特征。
*   **GNN 深度集成：** 使用两种架构（5 层 GIN-VN 和 8 层 GIN-VN-deep）。为了消除方差，先将每个架构在 10 个种子下的预测结果进行**平均**，得到稳定的预测向量。
*   **秩混合（Rank Blending）：** 最终预测值 $\hat{y}$ 采用加权秩平均公式：
    $$\hat{y} = 0.88 \cdot \text{rank}(RF_{ens}) + 0.06 \cdot \text{rank}(GNN_{gin\_vn}) + 0.06 \cdot \text{rank}(GNN_{deep})$$
    使用秩（Rank）而非原始概率值，增强了模型对单模型输出单调变形的鲁棒性。

### 3. 实验设计
*   **数据集：** 使用 OGB 官方的 `ogbg-molhiv` 数据集（41,127 个分子），采用标准的骨架划分（Scaffold Split）。
*   **Benchmark：** 对标 OGB 官方排行榜。
*   **对比方法：** 包括 HyperFusion（原榜单第一）、PAS+FPs、GIN 等主流 GNN 模型。
*   **消融实验：** 验证了 `max_features` 设置、GNN 混合比例、GNN 种子平均策略以及不同 GNN 架构对最终 AUC 的贡献。

### 4. 资源与算力
论文明确列出了算力消耗（基于 104 核 CPU 服务器和 1 张 H100 GPU）：
*   **分子指纹计算：** 约 10 分钟（CPU）。
*   **GNN 训练：** 每个架构约 30 分钟（GPU，10 个种子）。
*   **RF 训练：** 每个种子约 5 小时（50 核 CPU 并行，12 个模型 × 2 万棵树）。
*   **总计：** 完成一次完整的 10 种子评估约需 **50 小时**。

### 5. 实验数量与充分性
*   **实验规模：** 遵循 OGB 规范进行了 10 个随机种子的实验，并提供了每个种子的详细结果。
*   **充分性：** 进行了详尽的超参数搜索（表 6），涵盖了特征采样率、类别权重、混合权重等。
*   **客观性：** 对比了 7 种不同的 GNN 架构，并进行了严谨的消融实验，证明了提升并非偶然，而是源于特定的策略组合。

### 6. 主要结论与发现
*   **RF 的优越性：** 在骨架划分任务中，基于药效团指纹的随机森林比 GNN 具有更好的泛化基础。
*   **关键参数：** 发现 `max_features=0.20` 是提升 RF 性能的单一最大因素（+0.008 AUC）。
*   **方差消除：** 通过在混合前平均 GNN 种子预测，将最终结果的标准差降低了 5 倍（从 0.0008 降至 0.0002）。
*   **互补性：** 尽管 GNN 独立表现较弱（AUC ~0.79），但它捕捉到的拓扑信息能与指纹特征互补，提供 12% 的有效增益。

### 7. 优点
*   **性能卓越：** 取得了 OGB 榜单第一，且稳定性（方差）极高。
*   **无需预训练：** 不依赖大规模外部数据或复杂的自监督预训练。
*   **工程洞察：** 提供了关于随机森林在稀疏分子数据上调优的实用经验（如特征采样比例）。

### 8. 不足与局限
*   **计算开销：** 推理时需要运行 24 万棵树和 20 个 GNN 模型，内存和计算资源需求较高。
*   **泛化验证：** 实验主要集中在 `ogbg-molhiv` 一个数据集上，`max_features=0.20` 等发现是否适用于其他分子任务尚待验证。
*   **领先幅度：** 相比第二名（HyperFusion）的领先优势极小（+0.0001），属于极限调优的结果。

（完）
