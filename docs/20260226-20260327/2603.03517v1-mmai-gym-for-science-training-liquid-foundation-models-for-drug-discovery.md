---
title: "MMAI Gym for Science: Training Liquid Foundation Models for Drug Discovery"
title_zh: MMAI Gym for Science：为药物研发训练液态基础模型
authors: "Maksim Kuznetsov, Zulfat Miftahutdinov, Rim Shayakhmetov, Mikolaj Mizera, Roman Schutski, Bogdan Zagribelnyy, Ivan Ilin, Nikita Bondarev, Thomas MacDougall, Mathieu Reymond, Mihir Bafna, Kaeli Kaymak-Loveless, Eugene Babin, Maxim Malkov, Mathias Lechner, Ramin Hasani, Alexander Amini, Vladimir Aladinskiy, Alex Aliper, Alex Zhavoronkov"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03517v1"
tags: ["query:molrna"]
score: 9.0
evidence: 用于药物发现和分子数据的基础模型
tldr: 针对通用大模型在药物研发中表现不佳的问题，本文推出了 MMAI Gym for Science 平台，提供分子数据格式、多模态任务及训练方案。利用该平台训练出的 Liquid Foundation Model (LFM) 在分子优化、ADMET 预测等关键任务上，以更小的参数规模超越了大型通用模型，实现了高效且专业的药物研发支持。
selection_source: fresh_fetch
motivation: 通用大语言模型在处理复杂的分子科学任务时，往往缺乏必要的专业理解和性能表现。
method: 开发了 MMAI Gym for Science 平台，并利用其训练了一款高效的 Liquid Foundation Model (LFM)。
result: 该模型在分子优化、逆合成及属性预测等多个药物研发核心任务中，性能优于更大规模的通用或专用模型。
conclusion: 针对特定领域训练的小型基础模型在科学发现任务中比通用大模型更具效率和性能优势。
---

## 摘要
依赖上下文学习（in-context learning）的通用大语言模型（LLMs）无法可靠地提供药物研发任务所需的科学理解和性能。仅仅增加模型规模或引入推理标记（reasoning tokens）并不能带来显著的性能提升。为了弥补这一差距，我们推出了 MMAI Gym for Science，这是一个集分子数据格式与模态、特定任务推理、训练及基准测试方案于一体的一站式平台，旨在教导基础模型掌握“分子语言”，以解决实际的药物研发问题。我们利用 MMAI Gym 为这些应用训练了一个高效的液态基础模型（LFM），证明了规模较小、经过专门训练的基础模型在分子基准测试中可以超越规模大得多的通用模型或专业模型。在包括分子优化、ADMET 属性预测、逆合成分析、药物-靶点活性预测和官能团推理在内的关键药物研发任务中，所得模型达到了接近专家级的性能，且在大多数场景下超越了更大的模型，同时在该领域保持了更高的效率和更广泛的适用性。

## Abstract
General-purpose large language models (LLMs) that rely on in-context learning do not reliably deliver the scientific understanding and performance required for drug discovery tasks. Simply increasing model size or introducing reasoning tokens does not yield significant performance gains. To address this gap, we introduce the MMAI Gym for Science, a one-stop shop molecular data formats and modalities as well as task-specific reasoning, training, and benchmarking recipes designed to teach foundation models the 'language of molecules' in order to solve practical drug discovery problems. We use MMAI Gym to train an efficient Liquid Foundation Model (LFM) for these applications, demonstrating that smaller, purpose-trained foundation models can outperform substantially larger general-purpose or specialist models on molecular benchmarks. Across essential drug discovery tasks - including molecular optimization, ADMET property prediction, retrosynthesis, drug-target activity prediction, and functional group reasoning - the resulting model achieves near specialist-level performance and, in the majority of settings, surpasses larger models, while remaining more efficient and broadly applicable in the domain.

---

## 论文详细总结（自动生成）

这是一份关于论文《MMAI Gym for Science: Training Liquid Foundation Models for Drug Discovery》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：通用大语言模型（LLMs）虽然在自然语言处理上表现优异，但在药物研发（如分子属性预测、逆合成分析等）中表现不佳。单纯增加模型参数规模或引入通用的“思维链”推理并不能显著提升其在科学领域的专业性能。
*   **研究背景**：药物研发需要模型理解复杂的“分子语言”、具备跨模态推理能力，并能应对严重的分布外（OOD）数据挑战。
*   **整体含义**：本文提出了 **MMAI Gym for Science**，这是一个专门为科学领域设计的训练与评估环境，旨在通过高质量的领域数据、特定的推理路径和高效的架构，将通用模型转化为药物研发专家。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：结合高效的混合模型架构（Liquid Foundation Model, LFM）与领域特定的多阶段微调（SFT + RFT），使模型掌握分子的结构-属性关系。
*   **关键技术细节**：
    *   **LFM2-2.6B 架构**：采用混合架构，主要由**门控短卷积（ShortConv）**构成（处理局部序列特征，线性复杂度），辅以少量**分组查询注意力（GQA）**（处理全局交互）。这种设计在长上下文推理中比传统 Transformer 更高效。
    *   **MMAI Gym 训练方案**：
        *   **数据增强**：在训练中随机进行 SMILES、SELFIES 和 IUPAC 名称的转换，并对分子字符串进行非规范化随机遍历，防止模型过拟合于特定语法。
        *   **多阶段微调**：
            1.  **SFT（监督微调）**：在包含 200 多个任务、6 大类别（2D/3D 分子、蛋白质、药物-基因交互等）的语料库上进行。
            2.  **RFT（强化学习微调）**：使用 **GRPO（群体相对策略优化）** 算法。
    *   **奖励函数设计**：
        *   **格式奖励**：确保模型输出正确的 `<think>` 和 `<answer>` 标签。
        *   **思考奖励**：鼓励模型生成更长的推理链（最高 5000 字符）。
        *   **任务特定奖励**：分类任务（准确率）、回归任务（负 RMSE）、分子生成（RDKit 合法性及基准匹配）。

### 3. 实验设计
*   **数据集与场景**：
    *   **分子优化**：使用 MuMO-Instruct 基准，评估模型在优化多个冲突属性（如脂溶性、毒性）时的成功率。
    *   **官能团推理**：使用 FGBench，测试模型对分子结构微调后属性变化的预测能力。
    *   **逆合成分析**：使用 USPTO-50K 和 URSA-expert-2026（包含 OOD 目标）。
    *   **ADMET 预测**：使用 TDC（Therapeutics Data Commons）基准，涵盖吸收、分布、代谢、排泄和毒性。
*   **对比方法**：
    *   **通用 LLM**：GPT-4/5 系列、Claude 3.5/4.5、Llama 3.1 (8B/70B)、Qwen 2.5/3。
    *   **化学专用 LLM**：ChemLLM、LlaSMol、TxGemma-27B。
    *   **任务特定专家模型**：如 Prompt-MolOpt、RetroDFM 等。

### 4. 资源与算力
*   **算力消耗**：
    *   **SFT 阶段**：总步数 100,000 步，每步处理约 314 万个 tokens。
    *   **RFT 阶段**：使用 GRPO 算法，batch size 为 128，最大序列长度 16,384。分子生成任务训练了 1,000 步。
    *   **硬件细节**：文中提到了使用 DeepSpeed Stage 3、梯度检查点（Gradient Checkpointing）和 BF16 精度进行训练，但**未明确说明具体的 GPU 型号（如 H100 或 A100）及具体卡数**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 200 多个子任务，实验设计非常广泛。
*   **充分性与客观性**：
    *   实验对比了从 2.6B 到 70B 不等规模的模型，跨度较大。
    *   引入了**数据去重系统**，确保训练集和测试集无重叠，增强了结果的可信度。
    *   通过多次采样（不同提示词模板、不同分子增强方式）并取中位数/众数来评估，减少了随机性干扰。
    *   **客观性**：不仅在公开数据集上测试，还在内部 OOD 数据集上验证，证明了模型的泛化能力。

### 6. 主要结论与发现
*   **规模不是唯一因素**：经过 MMAI Gym 训练的 2.6B 参数 LFM 模型，在多个任务上超越了参数量大 10 倍以上的通用模型（如 Llama-3.1-70B）和专用模型（如 TxGemma-27B）。
*   **推理链（Reasoning）的价值**：启用 `<think>` 推理过程显著提升了模型在复杂任务（如逆合成和回归预测）中的表现。
*   **多任务学习的协同效应**：多任务 RFT 训练出的“通用专家”模型在稳健性上优于单任务微调模型，证明了跨领域知识的互补性。

### 7. 优点（亮点）
*   **架构高效**：LFM 的亚线性复杂度使其在处理长科学序列（如蛋白质或复杂反应链）时具有极高的推理速度和低内存占用。
*   **系统性强**：MMAI Gym 提供了一套完整的从数据增强、格式隔离到强化学习奖励设计的科学训练范式。
*   **推理透明**：通过强化学习引导模型生成化学推理步骤，提高了模型决策的可解释性。

### 8. 不足与局限
*   **回归任务瓶颈**：在某些特定的 ADMET 回归任务（如清除率 Clearance）中，LFM 仍略逊于非 LLM 的传统专家模型，说明 LLM 在精确数值预测上仍有提升空间。
*   **数据依赖**：虽然使用了大量公开数据，但模型性能的提升也高度依赖于 Insilico Medicine 内部的高质量私有数据，这使得其他研究者难以完全复现其最高性能。
*   **算力细节缺失**：未披露具体的硬件资源消耗，难以评估该训练方案的经济成本。

（完）
