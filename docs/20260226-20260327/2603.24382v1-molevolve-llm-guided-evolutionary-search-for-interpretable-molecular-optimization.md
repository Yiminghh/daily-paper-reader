---
title: "MolEvolve: LLM-Guided Evolutionary Search for Interpretable Molecular Optimization"
title_zh: MolEvolve：用于可解释分子优化的 LLM 引导进化搜索
authors: "Xiangsen Chen, Ruilong Wu, Yanyan Lan, Ting Ma, Yang Liu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24382v1"
tags: ["query:molrna"]
score: 9.0
evidence: 大语言模型引导的分子发现进化搜索
tldr: 针对深度学习在化学领域缺乏可解释性及难以处理“活性悬崖”的问题，本文提出 MolEvolve 框架。该框架将分子发现重新定义为自主的前瞻性规划问题，利用大语言模型（LLM）引导进化搜索，生成可执行的化学符号操作库。结合蒙特卡洛树搜索（MCTS）和外部工具，MolEvolve 能够自主发现优化路径并提供透明的推理链，在分子优化和属性预测任务中表现优异。
selection_source: fresh_fetch
motivation: 传统深度学习方法难以解释分子结构微小变化导致的活性剧变（活性悬崖），且受限于预设的相似性原则。
method: 结合 LLM 的冷启动能力与 MCTS 的测试时规划，通过进化可执行的化学符号操作库来发现最优分子转化路径。
result: 实验证明 MolEvolve 在分子属性预测和优化任务上均优于现有基准模型，并能生成人类可读的化学见解。
conclusion: MolEvolve 通过自主进化的符号操作，实现了兼具高性能与高可解释性的分子发现与优化。
---

## 摘要
尽管深度学习在化学领域取得了成功，但其影响力受限于缺乏可解释性以及无法解决“活性悬崖”（activity cliffs）问题，即微小的结构差异会引发剧烈的性质转变。当前的表示学习受限于相似性原理，往往无法捕捉这些结构-活性不连续性。为此，我们提出了 MolEvolve，这是一个将分子发现重新表述为自主前瞻性规划问题的进化框架。与依赖人工工程特征或僵化先验知识的传统方法不同，MolEvolve 利用大语言模型（LLM）主动探索并进化一个可执行的化学符号操作库。通过利用 LLM 进行冷启动，并使用蒙特卡洛树搜索（MCTS）引擎结合外部工具（如 RDKit）进行测试时规划，该系统能够自主发现最优轨迹。这一过程进化出了透明的推理链，将复杂的结构转换转化为可操作的、人类可读的化学见解。实验结果表明，MolEvolve 的自主搜索不仅进化出了透明且人类可读的化学见解，而且在性质预测和分子优化任务中均优于基准模型。

## Abstract
Despite deep learning's success in chemistry, its impact is hindered by a lack of interpretability and an inability to resolve activity cliffs, where minor structural nuances trigger drastic property shifts. Current representation learning, bound by the similarity principle, often fails to capture these structural-activity discontinuities. To address this, we introduce MolEvolve, an evolutionary framework that reformulates molecular discovery as an autonomous, look-ahead planning problem. Unlike traditional methods that depend on human-engineered features or rigid prior knowledge, MolEvolve leverages a Large Language Model (LLM) to actively explore and evolve a library of executable chemical symbolic operations. By utilizing the LLM to cold start and an Monte Carlo Tree Search (MCTS) engine for test-time planning with external tools (e.g. RDKit), the system self-discovers optimal trajectories autonomously. This process evolves transparent reasoning chains that translate complex structural transformations into actionable, human-readable chemical insights. Experimental results demonstrate that MolEvolve's autonomous search not only evolves transparent, human-readable chemical insights, but also outperforms baselines in both property prediction and molecule optimization tasks.

---

## 论文详细总结（自动生成）

以下是对论文《MolEvolve: LLM-Guided Evolutionary Search for Interpretable Molecular Optimization》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管深度学习（如GNN）在化学领域表现出色，但存在两大痛点：一是**缺乏可解释性**（黑盒模型），难以解释特定子结构对性质的贡献；二是难以处理**“活性悬崖”**（Activity Cliffs），即分子结构的微小变化导致性质剧变，传统模型往往会过度平滑这种不连续性。
*   **核心挑战**：大语言模型（LLM）虽然具备语义理解力，但在处理定量物理性质时容易产生“数值幻觉”，且其连续的语义空间与离散的化学结构空间存在拓扑失配（表示-精度悖论）。
*   **整体含义**：论文提出 **MolEvolve** 框架，将分子优化重新定义为一个在可执行符号空间上的自主前瞻性规划问题，旨在结合 LLM 的推理能力与符号逻辑的严谨性。

### 2. 方法论
MolEvolve 结合了 LLM 的冷启动知识蒸馏与蒙特卡洛树搜索（MCTS）规划：
*   **冷启动符号知识蒸馏**：
    *   **知识合成**：利用 LLM 通过思维链（CoT）生成关于目标性质的文本描述先验（如“LogP 对溶解度至关重要”）。
    *   **符号落地**：LLM 充当程序员，将文本规则转化为可执行的 Python 代码（调用 RDKit 库）。
    *   **迭代自纠错**：通过执行反馈自动修复代码中的 Bug，确保生成的化学描述符或操作符是可运行的。
*   **LLM 引导的进化搜索（MCTS）**：
    *   **状态定义**：在预测任务中为特征集合，在优化任务中为分子 SMILES 字符串。
    *   **节点扩展**：LLM 作为“分子操作员”，根据冷启动阶段的规则建议高潜力的结构修改动作。
    *   **奖励函数**：$R(s) = R_{task}(s) + \lambda \cdot R_{heuristic}(s, \Phi) + \gamma \cdot R_{penalty}(s)$。结合了任务性能、符号规则一致性及结构约束（如相似度惩罚）。
    *   **闭环验证**：所有生成的分子或操作必须通过 RDKit 的化学合法性检查，无效路径会被立即剪枝。

### 3. 实验设计
*   **数据集**：
    *   **性质预测**：MoleculeNet 中的 5 个基准（ESOL, Lipophilicity, BACE, BBBP, HIV）。
    *   **分子优化**：ChemCoT-Bench（针对 LogP 和 QED 的优化）。
*   **对比方法**：
    *   **GNN 类**：GIN, Graphformer。
    *   **LLM 类**：RAG 方法（Automolco, MolRAG）、微调模型（Attrilens-mol）。
    *   **强推理模型**：GPT-4o, GPT-5, DeepSeek-R1, DeepSeek-V3 等。
*   **评估指标**：RMSE（回归）、ROC-AUC（分类）、改进值（$\Delta$）及成功率（SR%）。

### 4. 资源与算力
*   **模型使用**：实验使用了 Qwen2.5 (7B/32B)、GPT-4o 及 GPT-4o-mini 作为骨干模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练/搜索的总时长。由于该方法主要基于推理侧的搜索（Test-time planning），其开销主要集中在 LLM API 调用和 MCTS 迭代次数上（实验中提到预测任务迭代 100 次，优化任务迭代 30 次）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个预测数据集和 2 个核心优化任务，每项实验运行 3 次取平均值。
*   **消融实验**：详细对比了“仅 LLM”、“LLM + 冷启动”以及“完整 MolEvolve”的效果，验证了各模块的必要性。
*   **充分性评价**：实验设计较为全面，不仅有定量对比，还包含了超参数敏感性分析（$\lambda$ 和 $\gamma$ 的热力图）以及搜索轨迹的可视化案例。对比对象涵盖了从传统 GNN 到最新的推理型 LLM，体现了客观性和公平性。

### 6. 主要结论与发现
*   **性能超越**：MolEvolve 在性质预测上优于专门的 GNN 模型，在分子优化上显著超过了 DeepSeek-R1 等强推理模型（LogP 优化提升近 4 倍）。
*   **解决悖论**：通过离散的符号搜索，模型能够有效捕捉“活性悬崖”，避免了连续嵌入空间的平滑效应。
*   **模型不可知性**：搜索出的符号特征可以迁移到随机森林、XGBoost 等不同下游模型中，且均能提升性能。
*   **可解释性**：系统生成的每一步修改都有对应的化学规则依据，形成了人类可读的推理链。

### 7. 优点
*   **符号与语义结合**：将 LLM 的模糊推理转化为确定的代码执行，解决了幻觉问题。
*   **自主进化**：无需人工预设复杂的化学特征，系统能根据任务目标自主发现有效的化学描述符。
*   **透明度高**：提供了实时的、可追溯的优化轨迹，对化学家具有实际参考价值。

### 8. 不足与局限
*   **计算成本**：MCTS 涉及大量的 LLM 调用和外部工具验证，推理延迟和成本显著高于端到端的深度学习模型。
*   **工具依赖**：系统的上限受限于外部化学工具（如 RDKit）的功能覆盖范围，对于某些复杂的立体化学或新型反应可能支持不足。
*   **搜索空间限制**：虽然 LLM 引导了搜索，但在极大规模的化学空间中，MCTS 的采样效率仍可能面临挑战。

（完）
