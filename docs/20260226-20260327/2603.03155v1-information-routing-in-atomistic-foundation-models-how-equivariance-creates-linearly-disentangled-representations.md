---
title: "Information Routing in Atomistic Foundation Models: How Equivariance Creates Linearly Disentangled Representations"
title_zh: 原子级基础模型中的信息路由：等变性如何创建线性解耦表示
authors: Joshua Steier
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03155v1"
tags: ["query:molrna"]
score: 9.0
evidence: 原子基础模型中的信息路由与解耦表示
tldr: 本研究探讨了原子基础模型如何组织中间表示信息。作者提出了组合投影分解（CPD）方法，通过线性移除成分信号来探测几何残差。研究发现，以MACE为代表的张量积等变架构能产生线性解耦的表示，使几何信息在移除成分后极易通过线性探测获取，而传统描述符则呈非线性纠缠。此外，等变模型会将特定信号路由至特定的不可约表示通道。这种线性解耦特性提升了样本效率，为等变架构提供了除预测精度外的实际优势。
selection_source: fresh_fetch
motivation: 旨在揭示原子基础模型中间表示所编码的信息内容及其组织逻辑。
method: 引入组合投影分解（CPD），利用QR投影线性分离成分信号与几何残差，并结合线性探测进行分析。
result: 等变架构（如MACE）实现了高度的线性解耦，并能将特定物理信号路由至特定的不可约表示通道（如L=1）。
conclusion: 线性解耦的表示使得等变架构在下游任务中具有更高的样本效率，证明了等变性在特征组织上的优越性。
---

## 摘要
原子级基础模型在其中间表示中编码了什么，以及这些信息是如何组织的？我们引入了成分投影分解（Composition Projection Decomposition, CPD），该方法利用 QR 投影从学习到的表示中线性移除成分信号，并探测几何残差。通过对 QM9 分子和 Materials Project 晶体上的五个架构家族的八个模型进行研究，我们发现了一个解耦梯度：张量积等变架构（MACE）生成的表示在移除成分后，几何信息几乎完全可以通过线性方式获取（对于 HOMO-LUMO 能隙，$R^2_{\text{geom}} = 0.782$），而手工设计的描述符（ANI-2x）则以非线性方式纠缠相同的信息（在 Ridge 回归下 $R^2_{\text{geom}} = -0.792$；在 MLP 下 $R^2 = +0.784$）。MACE 通过不可约表示通道路由特定目标的信号——偶极矩路由至 $L = 1$，HOMO-LUMO 能隙路由至 $L = 0$——在相同的探测下，这种模式在 ViSNet 的向量-标量架构中并未观察到。我们表明，在投影残差上使用梯度提升树探测会产生系统性的虚高，在纯成分目标上恢复出 $R^2 = 0.68$--$0.95$，因此我们建议将线性探测作为主要指标。线性解耦的表示在线性探测下具有更高的样本效率，这表明等变架构除了原始预测精度之外，还具有实际优势。

## Abstract
What do atomistic foundation models encode in their intermediate representations, and how is that information organized? We introduce Composition Projection Decomposition (CPD), which uses QR projection to linearly remove composition signal from learned representations and probes the geometric residual. Across eight models from five architectural families on QM9 molecules and Materials Project crystals, we find a disentanglement gradient: tensor product equivariant architectures (MACE) produce representations where geometry is almost fully linearly accessible after composition removal ($R^2_{\text{geom}} = 0.782$ for HOMO-LUMO gap), while handcrafted descriptors (ANI-2x) entangle the same information nonlinearly ($R^2_{\text{geom}} = -0.792$ under Ridge; $R^2 = +0.784$ under MLP). MACE routes target-specific signal through irreducible representation channels -- dipole to $L = 1$, HOMO-LUMO gap to $L = 0$ -- a pattern not observed in ViSNet's vector-scalar architecture under the same probe. We show that gradient boosted tree probes on projected residuals are systematically inflated, recovering $R^2 = 0.68$--$0.95$ on a purely compositional target, and recommend linear probes as the primary metric. Linearly disentangled representations are more sample-efficient under linear probing, suggesting a practical advantage for equivariant architectures beyond raw prediction accuracy.

---

## 论文详细总结（自动生成）

### 论文总结：原子级基础模型中的信息路由与线性解耦表示

#### 1. 核心问题与整体含义
论文探讨了原子级基础模型（Atomistic Foundation Models）内部如何组织化学信息，特别是**成分（Composition）**与**几何结构（Geometry）**信息是相互纠缠还是线性可分的。研究动机在于，理解这种内部组织逻辑有助于评估模型在下游任务中的效率、透明度以及对物理规律的捕捉能力。作者试图揭示架构选择（如等变性）如何影响表示的解耦程度。

#### 2. 方法论：组合投影分解 (CPD)
*   **核心思想**：利用线性代数中的 QR 分解，将模型的中间表示空间显式划分为“成分子空间”和“几何残差空间”。
*   **技术细节**：
    *   设定表示矩阵为 $X$，成分特征矩阵为 $Z$（包含元素比例和原子计数）。
    *   对 $Z$ 进行 QR 分解得到正交基 $Q$。
    *   **成分组件**：$X_{comp} = QQ^\top X$。
    *   **几何残差**：$X_{geom} = (I - QQ^\top)X$。
*   **探测协议**：在移除线性成分信号后的残差 $X_{geom}$ 上，使用 Ridge 回归（线性探测）来衡量几何信息的保留程度（$R^2_{geom}$）。

#### 3. 实验设计
*   **数据集**：QM9 分子数据集（小分子）和 Materials Project (MP) 晶体数据集。
*   **对比方法**：涵盖 5 种架构家族的 8 个模型：
    *   **张量积等变**：MACE。
    *   **向量-标量等变**：ViSNet。
    *   **学习到的不变性**：SchNet。
    *   **方向不变性**：DimeNet++。
    *   **手工描述符**：ANI-2x。
    *   **其他**：ORB v2, CHGNet, SevenNet。
*   **基准测试**：预测 HOMO-LUMO 能隙、偶极矩、形成能、带隙等物理属性。

#### 4. 资源与算力
*   **硬件**：单台工作站，配备 1 块 **NVIDIA RTX A6000 (48 GB)** GPU 和 64 GB RAM。
*   **耗时**：总计算量约为 **20 GPU 小时**。
    *   特征提取：每个模型约 2-10 分钟。
    *   Ridge 探测：每个模型-属性对小于 5 分钟。
    *   部分模型（SchNet, DimeNet++）在 QM9 上训练了 30 轮（约 1 小时/模型）。

#### 5. 实验数量与充分性
*   **实验规模**：对每个模型进行了 **30 次重复的 5 折交叉验证**（共 150 个评分/模型），以确保统计显著性。
*   **对照实验**：
    *   **正向控制**：探测平均原子质量（纯成分属性），验证 CPD 是否完全移除了成分信号。
    *   **洗牌控制**：随机化标签与表示的配对，排除噪声拟合。
*   **充分性评价**：实验设计非常严谨，不仅跨越了分子和晶体两个领域，还深入到了不可约表示（irreps）通道的细粒度分析，客观性较高。

#### 6. 主要结论与发现
*   **解耦梯度**：存在明显的架构梯度。MACE（张量积等变）实现了最高的线性解耦（$R^2_{geom} = 0.782$），而 ANI-2x（手工描述符）则严重纠缠，线性探测失效（$R^2_{geom} = -0.792$），尽管其信息在非线性探测下依然存在。
*   **信息路由**：MACE 会将特定物理信号路由至特定通道：偶极矩（矢量属性）集中在 $L=1$ 通道，而 HOMO-LUMO 能隙（标量属性）集中在 $L=0$ 通道。
*   **探测器陷阱**：发现 **GBT（梯度提升树）等非线性探测器会产生系统性虚高**，即使在纯成分目标上也能“找回”已被移除的信号，因此建议以线性探测为主。
*   **样本效率**：线性解耦程度更高的表示在小样本学习下表现更好，收敛更快。

#### 7. 优点
*   **方法论创新**：提出的 CPD 方法简单且具有严格的数学闭式解，为模型可解释性提供了有力工具。
*   **细粒度分析**：首次对原子基础模型进行了不可约表示（irreps）级别的路由分析。
*   **警示意义**：揭示了非线性探测在解耦分析中的潜在误导性，对后续研究具有重要的方法论指导价值。

#### 8. 不足与局限
*   **预训练干扰**：MACE 的优异表现可能部分归功于其大规模的 MPTraj 预训练，而非纯粹的架构优势，实验未能完全剥离数据规模的影响。
*   **分子规模限制**：主要基于 QM9（原子数 $\le 9$），在更大、更复杂的生物大分子上的泛化性有待验证。
*   **池化效应**：研究主要针对均值池化后的全局表示，可能掩盖了原子级别的局部解耦特性。
*   **模型覆盖**：由于环境限制，未能包含 EquiformerV2 等其他重要架构。

（完）
