---
title: "STRIDE: Post-Training LLMs to Reason and Refine Bio-Sequences via Edit Trajectories"
title_zh: STRIDE：通过编辑轨迹对大语言模型进行后训练，以实现生物序列的推理与优化
authors: "Daiheng Zhang, Shiyang Zhang, Sizhuang He, Yangtian Zhang, Syed Asad Rizvi, David van Dijk"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03573v1"
tags: ["query:molrna"]
score: 9.0
evidence: 用于生物序列优化和设计的生成模型
tldr: STRIDE是一个针对生物序列优化的LLM后训练框架。它通过让模型生成包含原子编辑操作（插入、删除、替换）的可执行轨迹，将序列优化转化为可验证的推理过程。结合监督微调和策略优化，STRIDE在蛋白质和分子优化任务中显著提升了编辑成功率、多样性和可控性，克服了扩散模型和传统自回归模型在离散编辑和长程规划上的局限。
selection_source: fresh_fetch
motivation: 现有的扩散模型难以实现可控的离散编辑，而自回归LLM在处理带约束的生物序列长程编辑规划时表现不足。
method: 提出STRIDE框架，通过Levenshtein对齐的编辑轨迹进行监督微调，并结合基于组的策略优化，使LLM能够输出原子编辑指令作为推理链。
result: "在蛋白质荧光和分子优化实验中，STRIDE将蛋白质编辑成功率从42%提升至89%，同时显著增强了序列的新颖性和有效性。"
conclusion: 将生物序列优化建模为显式的编辑轨迹推理，能有效提升LLM在复杂约束下的序列精炼能力和生成质量。
---

## 摘要
离散生物序列优化需要在严格的语法约束下进行迭代优化。扩散模型提供了渐进式优化，但无法自然地提供可控的离散编辑操作；而自回归大语言模型（LLM）通常缺乏针对受限编辑的显式长程规划。我们提出了 STRIDE（通过内化去噪仿真的序列轨迹优化），这是一个后训练框架，旨在训练 LLM 输出原子级编辑（插入/删除/替换）的可执行轨迹，作为变长优化的可验证推理轨迹。STRIDE 将基于 Levenshtein 对齐的最短编辑演示的监督微调与基于组的策略优化相结合，使编辑轨迹与任务奖励对齐，同时保持连贯的编辑行为。在蛋白质荧光和指令条件分子优化任务中，STRIDE 将变长蛋白质编辑的成功率从 42% 提高到 89%，同时将新颖性从 47% 提高到 97%，且与各种基准模型相比，表现出更强的有效性和可控性。代码已发布于 https://github.com/daiheng-zhang/STRIDE。

## Abstract
Discrete biological sequence optimization requires iterative refinement under strict syntactic constraints. Diffusion models offer progressive refinement but do not naturally expose controllable discrete edit operations, while autoregressive LLMs often lack explicit long-horizon planning for constrained edits. We propose STRIDE (Sequence Trajectory Refinement via Internalized Denoising Emulation), a post-training framework that trains an LLM to emit executable trajectories of atomic edits (INSERT/DELETE/REPLACE) as a verifiable reasoning trace for variable-length refinement. STRIDE combines supervised fine-tuning on Levenshtein-aligned shortest edit demonstrations with group-based policy optimization to align edit trajectories with task rewards while preserving coherent editing behavior. Across protein fluorescence and instruction-conditioned molecular optimization, STRIDE improves variable-length protein editing success from 42% to 89% while increasing novelty from 47% to 97%, and yields stronger validity and controllability compared to diverse baselines. The code is published at https://github.com/daiheng-zhang/STRIDE.

---

## 论文详细总结（自动生成）

以下是对论文《STRIDE: Post-Training LLMs to Reason and Refine Bio-Sequences via Edit Trajectories》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
生物序列（如蛋白质和有机小分子）的优化通常是一个在巨大离散空间中进行的受限搜索问题。现有的生成范式存在局限性：
*   **扩散模型（Diffusion Models）：** 虽然擅长渐进式去噪，但在处理离散编辑操作（如插入和删除）时不够直观，且难以在中间步骤强制执行特定领域的语法约束。
*   **自回归大语言模型（LLMs）：** 虽能生成符合语法的序列，但在优化任务中往往表现得“近视”，缺乏实现长程目标所需的显式规划能力。
*   **研究动机：** 提出 **STRIDE** 框架，旨在结合 LLM 的结构化生成能力与迭代优化的思想，通过让模型生成显式的、可执行的“原子编辑轨迹”（INSERT/DELETE/REPLACE）作为推理链，实现对生物序列的精确精炼。

### 2. 方法论
STRIDE 将序列优化建模为编辑空间中的轨迹规划，采用两阶段后训练方案：
*   **核心思想：** 训练模型在输出最终序列前，先在 `<think>` 标签内生成一系列基于索引（Index-grounded）的原子编辑指令。
*   **关键技术细节：**
    1.  **最短编辑路径构建：** 利用 Levenshtein 动态规划算法对原始序列和目标序列进行比对，通过回溯（Backtracing）生成确定性的最短编辑脚本作为监督信号。
    2.  **第一阶段：监督微调（SFT）：** 在对齐的编辑轨迹上训练模型，使其内化“最小编辑”偏置和保持序列有效性的先验知识。
    3.  **第二阶段：功能对齐（GRPO）：** 使用基于组的策略优化（如 GRPO、GSPO 等）将编辑轨迹与特定任务奖励（如蛋白质荧光强度、分子药理性）对齐。
    4.  **奖励设计：** 奖励函数不仅考虑目标属性的提升，还包含编辑预算约束、序列有效性检查以及对非目标属性漂移的惩罚。

### 3. 实验设计
*   **数据集与场景：**
    1.  **蛋白质优化：** 使用 TAPE 荧光景观预测任务（avGFP 变体），包含替换、插入和删除操作。
    2.  **分子优化：** 使用 MEGA-MolEdit-522K（指令条件编辑）和 DrugAssist 测试集，涵盖 14 种优化条件（如渗透性、溶解度等）。
*   **对比方法（Baselines）：**
    1.  **通用基准：** 随机扰动（Random Perturbation）、零样本 LLM（Zero-Shot）。
    2.  **消融基准：** 传统 SFT（直接生成序列，无轨迹）、传统 GSPO（直接 RL，无轨迹）。
    3.  **领域特定模型：** 离散扩散模型（EvoDiff）、编辑流模型（Edit Flow）。
*   **评价指标：** 成功率（Success）、唯一性（Uniqueness）、新颖性（Novelty）、有效性（Validity）及非目标属性偏移率（Shift Rate）。

### 4. 资源与算力
*   **模型规模：** 主要基于 **Qwen3-14B** 架构进行实验，同时也对比了 Qwen3-4B。
*   **训练细节：** 使用了 LoRA 适配器（SFT 阶段 $r=32$，GRPO 阶段 $r=16$），采用 8-bit 量化和梯度检查点（Gradient Checkpointing）技术。
*   **算力说明：** 论文提到使用了 DeepSpeed ZeRO-2 策略进行分布式训练，但**未明确说明具体的 GPU 型号、数量及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模：** 蛋白质任务采样了 100 个候选序列进行统计；分子任务涵盖了 500 个源分子在 14 种目标下的 7,000 次测试实例。
*   **充分性：** 实验设计较为全面，不仅对比了不同生成范式（扩散 vs 自回归），还深入探讨了不同 RL 算法（GRPO, GSPO, CISPO）对结果的影响。
*   **客观性：** 论文通过消融实验证明了“编辑轨迹”这一中间步骤的必要性，并针对蛋白质任务中的“仅替换”和“变长编辑”两种场景分别进行了评估，实验对比具有较好的公平性。

### 6. 主要结论与发现
*   **性能飞跃：** 在变长蛋白质编辑中，STRIDE 将成功率从 42% 提升至 89%，新颖性从 47% 提升至 97%。
*   **可控性增强：** 在分子优化中，带有编辑轨迹的 RL 训练（STRIDE-GSPO）比直接生成序列的 RL 具有更好的稳定性，显著降低了非目标属性的意外漂移。
*   **规模效应：** 模型规模对索引定位的准确性至关重要，14B 模型在遵循复杂编辑索引方面的表现显著优于 4B 模型。

### 7. 优点
*   **可解释性：** 编辑轨迹作为“推理链”，使模型的优化过程透明化，用户可以清晰看到每一步修改。
*   **灵活性：** 能够自然地处理变长序列优化（插入/删除），这是许多固定长度扩散模型难以实现的。
*   **验证性：** 编辑脚本是可执行的，可以通过执行脚本是否能得到输出序列来验证模型推理的逻辑一致性。

### 8. 不足与局限
*   **Oracle 依赖：** 实验高度依赖于预训练的属性预测器（Oracle），如果预测器本身对插入/删除操作存在偏差，模型可能会学习到错误的优化方向。
*   **索引漂移风险：** 在长序列编辑中，如果模型在轨迹中间步骤出错，会导致后续所有编辑位置失效（尽管 14B 模型缓解了此问题）。
*   **计算开销：** 生成长推理轨迹会增加推理时的 Token 消耗和延迟。
*   **实验覆盖：** 尚未在真实的实验室湿实验中验证生成序列的生物活性，目前仍停留在干实验模拟阶段。

（完）
