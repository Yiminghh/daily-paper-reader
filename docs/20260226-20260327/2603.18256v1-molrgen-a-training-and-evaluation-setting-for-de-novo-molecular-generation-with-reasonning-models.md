---
title: "MolRGen: A Training and Evaluation Setting for De Novo Molecular Generation with Reasonning Models"
title_zh: MolRGen：一种基于推理模型的从头分子生成训练与评估设置
authors: "Philippe Formont, Maxime Darrin, Ismail Ben Ayed, Pablo Piantanida"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18256v1"
tags: ["query:molrna"]
score: 9.0
evidence: 基于推理大语言模型的从头分子生成
tldr: 本研究针对推理型大语言模型在从头分子生成中缺乏无监督训练与评估框架的问题，提出了 MolRGen 基准。该基准包含大规模数据集、全新的多样性感知 top-k 评分指标，以及一套完整的训练评估流程。通过强化学习训练 24B 参数模型，验证了推理模型在优化分子属性及保持多样性方面的潜力，为药物发现提供了新的工具和评估标准。
selection_source: fresh_fetch
motivation: 现有的分子生成研究多依赖有监督学习，缺乏针对无先验知识的从头分子生成任务的推理模型训练与评估体系。
method: 提出了 MolRGen 基准，引入了多样性感知 top-k 评分机制，并利用强化学习对 24B 参数的推理模型进行训练。
result: 构建了大规模训练数据集，并证明了该设置能有效提升推理模型在分子生成中的性能，同时兼顾了分子的质量与多样性。
conclusion: MolRGen 为利用推理模型进行从头分子生成提供了标准化的训练与评估框架，填补了该领域的研究空白。
---

## 摘要
基于推理的大语言模型（LLMs）的最新进展在复杂问题解决任务中展现了显著的提升。受这些进展的启发，多项研究探索了推理型 LLM 在药物发现和分子设计中的应用。然而，大多数现有方法要么侧重于评估，要么依赖于需要真实标签（ground-truth labels）的训练设置，例如具有已知属性修改的分子对。这种监督在从头（de novo）分子生成中是不可用的，因为其目标是在没有高分候选分子先验知识的情况下，生成能够优化期望评分的新颖分子。为了填补这一空白，我们推出了 MolRGen，这是一个用于在从头分子生成任务上训练和评估推理型 LLM 的大规模基准和数据集。我们的贡献主要有三个方面：首先，我们提出了一种用于评估和训练从头分子生成及属性预测模型的设置；其次，我们引入了一种新颖的、具有多样性感知能力的 top-k 评分，该评分能够同时捕捉生成分子的质量和多样性；第三，我们展示了该设置可用于训练分子生成的 LLM，通过强化学习训练了一个 24B 参数的 LLM，并对其性能和局限性进行了详细分析。

## Abstract
Recent advances in reasoning-based large language models (LLMs) have demonstrated substantial improvements in complex problem-solving tasks. Motivated by these advances, several works have explored the application of reasoning LLMs to drug discovery and molecular design. However, most existing approaches either focus on evaluation or rely on training setups that require ground-truth labels, such as molecule pairs with known property modifications. Such supervision is unavailable in \textit{de novo} molecular generation, where the objective is to generate novel molecules that optimize a desirability score without prior knowledge of high-scoring candidates. To bridge this gap, we introduce MolRGen, a large-scale benchmark and dataset for training and evaluating reasoning-based LLMs on \textit{de novo} molecular generation. Our contributions are threefold. First, we propose a setting to evaluate and train models for \textit{de novo} molecular generation and property prediction. Second, we introduce a novel diversity-aware top-$k$ score that captures both the quality and diversity of generated molecules. Third, we show our setting can be used to train LLMs for molecular generation, training a 24B LLM with reinforcement learning, and we provide a detailed analysis of its performance and limitations.

---

## 论文详细总结（自动生成）

### 论文总结：MolRGen —— 基于推理模型的从头分子生成训练与评估设置

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：推理型大语言模型（Reasoning LLMs，如 DeepSeek-R1、Qwen-Thinking）在数学和编程领域表现卓越，但在药物发现（尤其是从头分子生成）中应用受限。
*   **核心问题**：现有的分子 LLM 研究多依赖于有监督学习（如分子对的属性修改），但在**从头分子生成（De Novo Generation）**场景下，目标是生成优化特定属性的新分子，此时并没有“正确答案”作为标签。
*   **研究意义**：论文提出了 **MolRGen** 基准，旨在探索是否能将推理模型在逻辑任务上的成功复制到分子设计中，通过强化学习（RL）和可验证的奖励信号（如对接评分、物理化学属性）来驱动模型进化。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：构建一个包含大规模蛋白质靶点和分子属性预测任务的环境，利用强化学习（GRPO 算法）训练模型，使其在生成分子时进行多步推理。
*   **关键技术细节**：
    *   **提示词结构**：定义为 $q = \{\nu_i, \rho_i, x_i, \sigma_i\}$，包含任务目标（最大化/最小化）、分子属性（如对接评分、QED、logP）、参考分子及阈值。
    *   **奖励设计**：采用**几何平均值**聚合多个属性的得分，确保模型必须同时满足所有约束（若任一属性得分为 0，总奖励即为 0）。
    *   **多样性感知 Top-k 评分（Diversity-aware top-k）**：这是本文的重要创新。它不仅衡量前 k 个分子的最高奖励，还引入了 Tanimoto 相似度约束（$s_{max}$），通过贪婪算法筛选出既高分又互不相似的分子集，从而评估模型的化学空间探索能力。
    *   **训练算法**：使用 **GRPO（Group Relative Policy Optimization）**，通过组内相对奖励来优化策略，无需额外的价值模型（Critic）。

#### 3. 实验设计
*   **数据集/场景**：
    *   **从头生成**：基于 SAIR 和 SIU 数据集提取了 4.5k 个蛋白质结构（靶点），包含 49k 训练提示词。
    *   **属性预测**：从 Polaris 平台整合了 27 个基准（如 ADMET 属性），包含 52k 个提示词。
    *   **逆合成**：包含 50k 条化学反应路径（作为未来扩展）。
*   **对比方法（Benchmark）**：
    *   **通用 LLM**：MiniMax-M2、Qwen3-30B/80B-Thinking、DeepSeek-R1 (Llama-70B/Qwen-32B 蒸馏版)、Gemma-3、Llama-3.3 等。
    *   **化学专用模型**：ChemDFM-R、ChemDFM-v2.0、Ether0。
    *   **自研模型**：RL-Mistral（基于 Mistral-Small-24B 使用 GRPO 训练）。

#### 4. 资源与算力
*   **算力支持**：论文提到实验得到了 CalculQuebec、加拿大数字研究联盟以及 MistralAI 的支持。
*   **具体配置**：
    *   **训练参数**：RL-Mistral 训练了 200 个 step，batch size 为 1024（128 个 prompt $\times$ 每组 8 个生成结果）。
    *   **未明确说明的部分**：文中未具体列出使用的 GPU 型号（如 H100 或 A100）的具体数量及总训练时长，但提到由于实验成本高昂，仅探索了有限的配置。

#### 5. 实验数量与充分性
*   **实验规模**：评估了超过 10 种主流开源模型，涵盖了从 14B 到 229B 的不同参数规模。
*   **任务覆盖**：实验涵盖了单目标/多目标分子生成、回归预测、分类预测等多个维度。
*   **充分性评价**：实验设计较为全面，特别是引入了不同相似度阈值下的多样性分析。然而，由于 RL 训练成本极高，论文仅展示了初步的 RL 训练效果，未进行大规模的超参数消融实验。

#### 6. 主要结论与发现
*   **推理能力的迁移**：通用推理模型（如 MiniMax-M2、Qwen3）在分子生成任务上表现优异，甚至能与参数量较小的化学专用模型持平。
*   **RL 训练的利弊**：通过 GRPO 训练的 RL-Mistral 在 **Top-1 奖励**上显著超过所有基准模型，证明了 MolRGen 环境能提供有效的学习信号。
*   **多样性塌缩**：RL 训练虽然提升了峰值性能，但导致了严重的“模式崩溃”（Mode Collapse），模型倾向于针对同一提示词生成重复的分子，导致其在多样性感知指标上表现较差。
*   **属性预测挑战**：所有 LLM 在回归任务（如溶解度预测）上表现普遍较弱，但在分类任务上，通用推理模型展现出了一定的跨领域迁移能力。

#### 7. 优点
*   **填补空白**：首次为推理型 LLM 提供了大规模、可验证的从头分子生成训练与评估框架。
*   **指标创新**：提出的“多样性感知 Top-k 评分”比传统的 Top-k 或单一多样性指标更符合药物发现的实际需求（即寻找多个不同的候选药物）。
*   **数据质量**：整合了高质量的蛋白质结构数据和工业级的 ADMET 基准，具有较强的实用价值。

#### 8. 不足与局限
*   **代理奖励的局限性**：实验依赖于对接评分（Docking Score）等计算模拟值，这些指标与实际的生物活性（Binding Affinity）之间存在已知偏差。
*   **探索能力不足**：目前的 RL 训练设置未能解决多样性丧失的问题，模型容易陷入局部最优。
*   **安全对齐干扰**：研究发现某些模型（如 Ether0）由于过度的安全对齐，会将合法的分子生成请求误判为“制造化学武器”而拒绝回答。
*   **算力门槛**：大规模蛋白质对接和 RL 训练对算力要求极高，限制了该基准在普通研究机构中的普及。

（完）
