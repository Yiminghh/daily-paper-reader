---
title: Fast and Versatile RNA Design via Motif-level Divide-and-Conquer and Structure-level Rival Search
title_zh: 通过基序级分治和结构级竞争搜索实现快速且通用的RNA设计
authors: "Tianshuo Zhou, David H. Mathews, Liang Huang"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02283v1"
tags: ["query:molrna"]
score: 9.0
evidence: 通过基元级分治进行RNA设计
tldr: 本研究提出了一种快速且通用的RNA设计算法，旨在解决现有方法在最小自由能（MFE）和系综指标上难以兼顾的问题。该方法采用分治策略，将目标结构分解为基元树，利用计算语言学中的立方剪枝技术递归组合序列，并通过结构级竞争搜索抑制错误折叠。实验表明，该方法在RNAsolo和Eterna100基准测试中达到SOTA水平，并在长链RNA设计上表现出极高的效率和准确性。
selection_source: fresh_fetch
motivation: 现有的RNA设计方法在计算效率上存在挑战，且难以在最小自由能（MFE）和系综指标之间取得平衡。
method: 通过基元级分治策略将结构分解为树状子目标，利用立方剪枝递归组合序列，并引入结构级竞争搜索来抑制错误折叠。
result: 该方法在Eterna100等基准测试中达到SOTA，并显著提升了16S rRNA长链设计的折叠概率和运行速度。
conclusion: 该算法通过创新的分治与搜索机制，为高效、精准的复杂RNA序列设计提供了一个通用的解决方案。
---

## 摘要
RNA设计旨在识别能够折叠成目标二级结构的RNA序列。该任务在计算效率方面具有挑战性。大多数现有方法要么专注于基于最小自由能（MFE）的指标，要么专注于基于系综（ensemble）的指标，缺乏一种在两者上均表现良好的统一方法。受我们先前关于RNA结构和基序（即连续结构环的集合）不可设计性研究的启发，我们提出了一种快速且通用的RNA设计算法。我们的方法将目标结构分解为一棵子目标树，其中每个叶节点对应一个基序，每个内部节点对应一个子结构。我们首先为每个基序设计部分序列，然后借鉴计算语言学中的立方体剪枝（cube pruning）策略，有选择地递归组合这些部分序列，从而实现对基于系综指标的有效优化。最后，一种新颖的全结构竞争搜索进一步优化序列，以抑制错误折叠的备选结构并增强基于MFE的性能。我们的方法效率极高，并在原生RNAsolo结构和Eterna100基准测试中达到了最先进的结果，在基于系综和基于MFE的指标上均表现优异。此外，它显著改进了源自16S rRNA的长结构基准设计，将平均折叠概率从0.18提高到0.39，且速度提升了一个数量级，证明了其有效性和可扩展性。源代码和数据可在以下网址获取：https://github.com/shanry/FastDesign。

## Abstract
RNA design aims to identify RNA sequences that fold into a target secondary structure. This task is challenging in terms of computational efficiency. Most existing methods focus on either minimum free energy (MFE)-based or ensemble-based metrics, leaving a gap for a unified approach that performs well across both. We introduce a fast and versatile RNA design algorithm inspired by our previous work on the undesignability of RNA structures and motifs (i.e., sets of contiguous structural loops). Our approach decomposes a target structure into a tree of sub-targets where each leaf node corresponds to a motif and each internal node corresponds to a substructure. We first design partial sequences for each motif, then these partial sequences are selectively and recursively combined via the cube pruning strategy borrowed from computational linguistics, enabling effective optimization of ensemble-based metrics. Finally, a novel whole-structure rival search further refines sequences to suppress misfolded alternatives and enhance MFE-based performance. Our method is highly efficient and also achieves state-of-the-art results on native RNAsolo structures and the Eterna100 benchmark, excelling in both ensemble- and MFE-based metrics. Additionally, it substantially improves the design of long-structure benchmark derived from 16S rRNA, increasing average folding probability from 0.18 to 0.39 with an order-of-magnitude speedup, demonstrating its effectiveness and scalability. Availability: Source code and data are available at: https://github.com/shanry/FastDesign.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FastDesign** 的新型 RNA 设计算法，旨在通过结合基序级（Motif-level）的分治策略和结构级（Structure-level）的竞争搜索，解决 RNA 逆折叠问题中的效率与多指标优化平衡难题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：RNA 设计（即给定目标二级结构，寻找能折叠成该结构的序列）是一个 NP-hard 问题。
*   **研究动机**：
    *   **效率瓶颈**：现有基于优化的方法需要反复调用计算复杂度为 $O(n^3)$ 的折叠算法，设计长链 RNA 时速度极慢。
    *   **指标失衡**：多数方法要么侧重于最小自由能（MFE）准则，要么侧重于系综概率（Ensemble probability），很难在两者上同时达到最优。
    *   **缺乏可解释性**：现有方法难以解释为何某些结构“不可设计”。
*   **整体含义**：本文通过引入“设计性（Designability）”理论，将复杂结构拆解并高效组合，提供了一个兼顾速度、系综稳定性和 MFE 准确性的通用框架。

### 2. 方法论：核心思想与关键技术
该方法分为两个主要阶段：**基序级分治组合** 和 **结构级竞争搜索**。

*   **基序级分治（Divide-Conquer-Combine）**：
    *   **分治（Divide）**：基于设计性理论，在“易于设计”的堆叠碱基对处将目标结构分解为树状结构。叶节点为基本基序（Motif），内部节点为子结构。
    *   **征服（Conquer）**：利用适配版的 SAMFEO 算法为叶节点基序独立设计候选序列。
    *   **组合（Combine - 立方体剪枝）**：借鉴计算语言学中的**立方体剪枝（Cube Pruning）**算法。在组合子结构序列时，不进行穷举，而是利用线性加和的概率作为启发式得分，在搜索空间中高效筛选 Top-K 候选组合，极大地减少了全局折叠评估的次数。
*   **结构级竞争搜索（Rival Search）**：
    *   **核心思想**：如果初步设计的序列 MFE 结构不是目标结构，则识别出那个更稳定的“竞争结构（Rival Structure）”。
    *   **技术细节**：计算目标结构与竞争结构之间的能量差（$\Delta\Delta G$）。由于能量差仅取决于少数“差异位点”，算法可以快速枚举这些位点的碱基组合，寻找能使目标结构能量低于竞争结构的序列，从而精确修复 MFE 缺陷。

### 3. 实验设计
*   **数据集**：
    *   **RNAsolo764**：包含 764 个源自 PDB 3D 结构的真实原生 RNA 结构。
    *   **ArchiveII100**：100 个多样化的原生 RNA 结构。
    *   **Eterna100**：由人类玩家设计的 100 个具有挑战性的非原生结构基准。
    *   **16S-MFE**：包含 10 个长度超过 1000nt 的长链 RNA 结构（源自 16S rRNA）。
*   **对比方法（Baselines）**：包括经典的 RNAinverse、NUPACK、NEMO、m2dRNAs 以及作者之前的 SAMFEO 算法。
*   **评估指标**：折叠概率 $p(y^*|x)$、归一化系综缺陷（NED）、MFE/uMFE 成功求解数量、运行时间。

### 4. 资源与算力
*   **硬件环境**：实验在 Linux 系统上运行，配置为 **AMD EPYC 9474F 处理器** 和 **16 GB 内存**。
*   **算力性质**：该算法主要依赖 CPU 计算，未提及 GPU 加速。
*   **训练/运行时长**：
    *   在 RNAsolo 数据集上，FastDesign 平均耗时 37.6 秒，而 XFastDesign（快速版）仅需 13.3 秒。
    *   在长链 16S rRNA 设计中，FastDesign 比 SAMFEO 快了一个数量级（约 2612 秒 vs 56747 秒）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从短链（原生/人工）到极长链（>1000nt）的多种场景，每种结构运行 5 次以保证统计意义。
*   **消融实验**：专门针对“竞争搜索（Rival Search）”模块和“立方体剪枝大小（k）”进行了消融研究，验证了各组件对 MFE 成功率和效率的贡献。
*   **公平性**：对比了多种不同类型的基准算法，并针对 NUPACK 使用了特定的折叠引擎版本进行公平对比。实验设计全面且客观。

### 6. 主要结论与发现
*   **性能领先**：FastDesign 在所有基准测试（RNAsolo, Eterna100）中均取得了 SOTA 结果，尤其是在 MFE 求解数量和系综概率上表现均衡。
*   **长链优势**：在处理 >1000nt 的长结构时，FastDesign 将平均折叠概率从 0.18 提升至 0.39，且运行速度提升了 10 倍以上。
*   **效率卓越**：XFastDesign 变体在保持高成功率的同时，速度远超现有主流工具。
*   **理论验证**：证明了通过局部基序的设计性来指导全局结构设计是行之有效的。

### 7. 优点
*   **算法创新**：首次将 NLP 中的立方体剪枝引入 RNA 设计，巧妙解决了子结构组合时的组合爆炸问题。
*   **多指标统一**：通过分治优化系综指标，通过竞争搜索优化 MFE 指标，实现了两者的有机结合。
*   **可扩展性强**：分治策略使得该算法在处理超长 RNA 序列时具有天然的计算优势。
*   **理论支撑**：分解策略基于严谨的能量模型分析（定理 1），具有较强的科学解释性。

### 8. 不足与局限
*   **依赖底层模型**：算法性能仍受限于底层热力学模型（如 ViennaRNA 的 Turner 模型）的准确性。
*   **竞争结构选择**：目前的竞争搜索主要针对最显著的竞争结构，对于存在大量近简并（near-degenerate）错误折叠结构的情况，可能需要更复杂的迭代。
*   **伪结限制**：该方法目前主要针对不含伪结（pseudoknot-free）的二级结构，尚未扩展到更复杂的伪结设计。

（完）
