---
title: Reinforcement Learning with LLM-Guided Action Spaces for Synthesizable Lead Optimization
title_zh: 基于大语言模型引导动作空间的强化学习，用于可合成的先导化合物优化
authors: "Tao Li, Kaiyuan Hou, Tuan Vinh, Monika Raj, Zhichun Guo, Carl Yang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07669v1"
tags: ["query:molrna"]
score: 8.0
evidence: LLM引导的分子发现先导化合物优化
tldr: 针对药物发现中先导化合物优化难以兼顾性质提升与可合成性的挑战，本文提出 MolReAct 框架。该框架将优化过程建模为受合成约束的马尔可夫决策过程，利用工具增强的 LLM 智能体识别反应位点并推荐基于模板的转化动作，并采用 GRPO 强化学习算法训练策略模型以最大化长期奖励。实验表明，MolReAct 在多项任务中显著优于现有基准，在提升分子性质的同时确保了明确的合成路径。
selection_source: fresh_fetch
motivation: 现有的先导化合物优化方法往往在追求高性质得分时忽略了分子的可合成性，或因搜索空间巨大而效率低下。
method: 提出 MolReAct 框架，结合工具增强的 LLM 智能体生成受反应模板约束的动作空间，并利用 GRPO 强化学习算法优化多步反应轨迹。
result: "在 14 项优化任务中，MolReAct 的平均 Top-10 得分比最强可合成基准提升了 10.4%，且在多数任务中展现出最优的样本效率。"
conclusion: 通过将每一步优化都锚定在经过验证的反应模板上，MolReAct 能够生成兼具优异性质和明确合成路径的候选分子。
---

## 摘要
药物发现中的先导化合物优化要求在提高治疗特性的同时，确保所提出的分子修饰对应于可行的合成路线。现有方法要么优先考虑属性评分而未强制要求可合成性，要么依赖于对庞大反应网络进行昂贵的枚举，而直接应用大语言模型（LLMs）则经常产生化学上无效的结构。我们提出了 MolReAct，这是一个将先导化合物优化建模为马尔可夫决策过程的框架，其动作空间受限于由经过验证的反应模板定义的合成约束。一个工具增强的 LLM 智能体充当动态反应环境，调用专门的化学分析工具来识别反应位点，并根据匹配的模板提出具有化学依据的转化方案。通过群体相对策略优化（GRPO）训练的策略模型在这些受限动作中进行选择，以在多步反应轨迹中最大化长期预言机奖励。一种基于 SMILES 的缓存机制进一步将端到端优化时间缩短了约 43%。在来自 Therapeutic Data Commons 的 13 个属性优化任务和 1 个基于结构的分子对接任务中，MolReAct 实现了 0.563 的平均 Top-10 得分，相比最强的可合成基准模型实现了 10.4% 的相对提升，并在 14 个任务中的 10 个任务上达到了最佳样本效率。消融实验证实，工具增强的反应建议和轨迹级策略优化均贡献了互补的增益。通过将每一步都建立在经过验证的反应模板之上，MolReAct 产生的分子不仅属性得到了改善，且每个分子都附带明确的合成路径。

## Abstract
Lead optimization in drug discovery requires improving therapeutic properties while ensuring that proposed molecular modifications correspond to feasible synthetic routes. Existing approaches either prioritize property scores without enforcing synthesizability, or rely on expensive enumeration over large reaction networks, while direct application of Large Language Models (LLMs) frequently produces chemically invalid structures. We introduce MolReAct, a framework that formulates lead optimization as a Markov Decision Process over a synthesis-constrained action space defined by validated reaction templates. A tool-augmented LLM agent serves as a dynamic reaction environment that invokes specialized chemical analysis tools to identify reactive sites and propose chemically grounded transformations from matched templates. A policy model trained via Group Relative Policy Optimization (GRPO) selects among these constrained actions to maximize long-term oracle reward across multi-step reaction trajectories. A SMILES-based caching mechanism further reduces end-to-end optimization time by approximately 43%. Across 13 property optimization tasks from the Therapeutic Data Commons and one structure-based docking task, MolReAct achieves an average Top-10 score of 0.563, outperforming the strongest synthesizable baseline by 10.4% in relative improvement, and attains the best sample efficiency on 10 of 14 tasks. Ablations confirm that both tool-augmented reaction proposals and trajectory-level policy optimization contribute complementary gains. By grounding every step in validated reaction templates, MolReAct produces molecules that are property-improved and each accompanied by an explicit synthetic pathway.

---

## 论文详细总结（自动生成）

### 论文总结：MolReAct —— 基于大语言模型引导动作空间的强化学习用于可合成先导化合物优化

#### 1. 核心问题与整体含义（研究动机和背景）
在药物发现的先导化合物优化阶段，核心挑战在于**平衡分子性质的提升与化学合成的可行性**。
*   **现有痛点**：传统的强化学习（RL）或遗传算法（GA）往往只追求高性质得分，产生的分子常因结构过于复杂而无法合成；而现有的可合成性约束方法（如枚举反应网络）计算成本极高。
*   **LLM 的局限**：直接使用大语言模型（LLM）生成分子容易产生化学上无效的结构，且难以处理多步序列决策。
*   **论文目标**：提出 **MolReAct** 框架，通过将 LLM 的化学知识与经过验证的反应模板相结合，在确保每一步修饰都具备明确合成路径的前提下，高效优化分子性质。

#### 2. 方法论：核心思想与技术细节
MolReAct 将优化过程建模为**受限动作空间下的马尔可夫决策过程（MDP）**：
*   **动态反应环境（LLM Agent）**：
    *   使用工具增强的 LLM（如 Llama-3.3-70B）作为环境。
    *   **模板匹配**：首先从 115 个验证过的反应模板中筛选出与当前分子匹配的子集。
    *   **工具调用（ReAct）**：LLM 调用 RDKit 工具（如反应位点分析、官能团检测）分析分子结构。
    *   **动作提议**：LLM 根据目标性质，从匹配模板中提议最多 10 个具体的反应（包括选择模板和匹配的构建块/试剂）。
*   **策略模型与强化学习**：
    *   **策略网络**：采用 Qwen-3-4B 模型，通过线性头输出动作概率。
    *   **GRPO 算法**：使用**群体相对策略优化（Group Relative Policy Optimization）**。该算法通过对同一初始分子采样一组轨迹，利用组内相对奖励（标准化后的得分）进行训练，无需额外的价值函数（Critic）网络，有效处理多步信用分配。
*   **效率优化**：引入 **SMILES 缓存机制**，存储已探索分子的反应提议和结果，避免重复调用昂贵的 LLM 推理，将优化时间缩短约 43%。

#### 3. 实验设计
*   **数据集与任务**：
    *   **13 个 TDC 任务**：涵盖多参数优化（MPO）、分子重新发现（Rediscovery）和中值目标（Median）。
    *   **1 个对接任务**：针对可溶性环氧化物水解酶（sEH）的结构化对接评分。
*   **基准方法（Baselines）**：
    *   **可合成基准**：ReaSyn, SynFormer, mCLM。
    *   **通用/LLM 基准**：GraphGA, DrugAssist, LDMol。
*   **评价指标**：Top-10 平均得分（衡量优化上限）和 AUC 10（衡量样本效率/收敛速度）。

#### 4. 资源与算力
*   **硬件配置**：
    *   **策略模型训练**：1 张 NVIDIA RTX 6000 Ada GPU (48GB)。
    *   **环境 LLM 推理**：4 张 NVIDIA RTX 6000 Ada GPU（使用 vLLM 引擎部署 Llama-3.3-70B）。
*   **训练时长**：在开启缓存的情况下，单个任务的端到端优化约需 **40 小时**（若无缓存则需约 70 小时）。

#### 5. 实验数量与充分性
*   **实验规模**：在 14 个不同性质的任务上进行了测试，每个任务运行 3 次随机种子取平均值。
*   **消融实验**：
    *   对比了“随机模板”、“原生 LLM”与“工具增强 LLM”在单步提议上的质量差异。
    *   对比了“随机策略”、“贪婪搜索”与“GRPO 策略”在多步优化中的表现。
*   **可合成性验证**：对提议的构建块进行了 Enamine 商业库匹配分析（约 70% 可直接购买，其余 SA 评分较低，易于合成）。
*   **客观性**：实验设置了统一的 10,000 次预言机（Oracle）调用预算，对比公平，且引入了未参与训练的 AutoDock Vina 评分作为独立验证。

#### 6. 主要结论与发现
*   **性能领先**：MolReAct 在平均 Top-10 得分上达到 0.563，比最强的可合成基准 SynFormer 提升了 **10.4%**。
*   **效率卓越**：在 14 个任务中的 10 个任务上取得了最佳的样本效率（AUC 10）。
*   **化学合理性**：通过路径分析发现，模型能够引入关键药效团（如通过 N-烷基化或芳基氰化），在提升预言机得分的同时也改善了物理对接亲和力。
*   **合成透明度**：每个优化后的分子都自带一条由 115 个标准反应模板构成的明确合成路径。

#### 7. 优点与亮点
*   **双模型架构**：巧妙地将 LLM 的“化学常识/推理能力”用于构建动作空间，将轻量级策略模型的“决策能力”用于轨迹优化。
*   **工具增强**：通过 RDKit 工具解决了 LLM 幻觉问题，确保了分子结构的 100% 合法性。
*   **合成落地**：不同于只给结果的生成模型，MolReAct 提供的是“反应序列”，更符合药物化学家的实际工作流程。
*   **训练创新**：引入 GRPO 算法，简化了强化学习流程，同时通过缓存机制解决了 LLM 在 RL 环境中的推理瓶颈。

#### 8. 不足与局限
*   **模板库限制**：目前仅依赖 115 个预定义模板，虽然涵盖常见反应，但无法覆盖所有前沿合成手段。
*   **合成细节缺失**：仅验证了模板匹配，未考虑具体的反应条件（温度、溶剂）、区域选择性或保护基策略。
*   **计算成本**：尽管有缓存，但 40 小时的单任务优化时间对于大规模筛选仍显昂贵。
*   **预言机依赖**：优化结果高度依赖于计算预言机的准确性，若预言机本身存在偏差，模型可能会产生“过度拟合”计算指标而非实际生物活性的分子。

（完）
