---
title: Equivariant Efficient Joint Discrete and Continuous MeanFlow for Molecular Graph Generation
title_zh: 用于分子图生成的等变高效联合离散与连续 MeanFlow
authors: "Rongjian Xu, Teng Pang, Zhiqiang Dong, Guoqiang Wu"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08189v1"
tags: ["query:molrna"]
score: 10.0
evidence: 分子图生成的等变生成框架
tldr: 针对分子图生成中离散拓扑与连续几何分布不一致及采样效率低的问题，本文提出 EQUIMF 框架。该框架利用 SE(3) 等变性的同步 MeanFlow 动力学，实现了结构与几何的联合建模。通过引入统一时间桥、互调节机制及创新的离散 MeanFlow 参数化方案，EQUIMF 在保证物理一致性的同时显著提升了生成质量和采样效率，是处理复杂分子数据的有效生成模型。
selection_source: fresh_fetch
motivation: 现有的分子生成方法通常将结构与几何解耦，导致动力学不同步且采样效率低下，难以保证物理一致性。
method: 提出一种统一的 SE(3) 等变 MeanFlow 框架，通过互调节的平均速度更新和离散 MeanFlow 参数化实现同步生成。
result: 实验表明，EQUIMF 在生成质量、物理有效性和采样速度上均显著优于现有的扩散模型和流匹配方法。
conclusion: EQUIMF 为分子图的联合离散-连续生成提供了一种高效、物理一致且具备等变性的新范式。
---

## 摘要
图结构数据同时包含离散拓扑和连续几何，由于异构分布、不相容的噪声动力学以及对等变归纳偏置的需求，这给生成建模带来了根本性挑战。现有的用于图生成的流匹配（flow-matching）方法通常将结构与几何解耦，缺乏同步的跨域动力学，且依赖迭代采样，往往导致物理上不一致的分子构象和缓慢的采样速度。为了解决这些局限性，我们提出了等变 MeanFlow (EQUIMF)，这是一个统一的 SE(3) 等变生成框架，通过同步的 MeanFlow 动力学对离散和连续组件进行联合建模。EQUIMF 引入了统一的时间桥（time bridge）和平均速度更新，并在结构与几何之间进行相互调节，从而在保持物理一致性的同时实现高效的少步生成。此外，我们开发了一种具有简单且有效参数化的新型离散 MeanFlow 公式，以支持在离散图结构上的高效生成。广泛的实验表明，EQUIMF 在生成质量、物理有效性和采样效率方面始终优于先前的扩散和流匹配方法。

## Abstract
Graph-structured data jointly contain discrete topology and continuous geometry, which poses fundamental challenges for generative modeling due to heterogeneous distributions, incompatible noise dynamics, and the need for equivariant inductive biases. Existing flow-matching approaches for graph generation typically decouple structure from geometry, lack synchronized cross-domain dynamics, and rely on iterative sampling, often resulting in physically inconsistent molecular conformations and slow sampling. To address these limitations, we propose Equivariant MeanFlow (EQUIMF), a unified SE(3)-equivariant generative framework that jointly models discrete and continuous components through synchronized MeanFlow dynamics. EQUIMF introduces a unified time bridge and average-velocity updates with mutual conditioning between structure and geometry, enabling efficient few-step generation while preserving physical consistency. Moreover, we develop a novel discrete MeanFlow formulation with a simple yet effective parameterization to support efficient generation over discrete graph structures. Extensive experiments demonstrate that EQUIMF consistently outperforms prior diffusion and flow-matching methods in generation quality, physical validity, and sampling efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《Equivariant Efficient Joint Discrete and Continuous MeanFlow for Molecular Graph Generation》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：分子生成是化学、生物和材料科学的核心问题。分子数据具有双重属性：**离散的拓扑结构**（原子类型、键类型）和**连续的几何空间**（3D 坐标）。
*   **核心问题**：现有的生成模型（如扩散模型或流匹配方法）通常将结构与几何解耦处理，导致两者之间的动力学不一致，且往往依赖于多步迭代采样，导致生成效率低下且分子构象在物理上可能不一致。
*   **研究动机**：开发一个能够同时处理离散和连续分量、具备 SE(3) 等变性、且采样效率高的统一生成框架。

### 2. 论文提出的方法论：EQUIMF
*   **核心思想**：提出 **Equivariant MeanFlow (EQUIMF)** 框架，利用同步的 **MeanFlow（均值流）** 动力学联合建模离散结构和连续几何。
*   **关键技术细节**：
    *   **同步 MeanFlow 动力学**：不同于传统流匹配建模瞬时速度，MeanFlow 建模的是时间间隔 $[t, s]$ 内的**平均速度**。这允许使用更大的步长进行采样。
    *   **新型离散 MeanFlow**：针对离散域（原子/键类型），通过参数化平均转移速率矩阵（Average Transition Rate Matrix）而非瞬时速率，实现了高效的少步采样。
    *   **互调节机制（Mutual Conditioning）**：引入共享的 SE(3) 等变编码器 $\phi_{\theta1}$。离散结构的更新会参考几何信息，几何坐标的更新也会参考结构信息，确保了跨域的一致性。
    *   **SE(3) 等变性设计**：基于 EGNN（等变图神经网络）构建骨干网络，确保模型在旋转、平移和反射变换下保持几何一致性。
    *   **时间扭曲策略（Time Distortion）**：在采样过程中采用非均匀时间步长，在结构变化的敏感阶段增加采样密度。

### 3. 实验设计
*   **数据集**：
    *   **QM9**：包含 13.4 万个小有机分子，用于 3D 分子建模、生成及条件生成任务。
    *   **GEOM-DRUG**：包含大规模、更复杂的药物分子，用于测试模型处理复杂几何的能力。
    *   **合成数据集**：Planar、Tree、SBM，用于单独验证离散图结构的生成性能。
*   **Benchmark 与对比方法**：
    *   对比了多种 SOTA 模型，包括：ENF、G-Schnet、GDM、EDM（等变扩散模型）、EDM-Bridge 以及最新的 EQUIFM（2023 年的等变流匹配模型）。
*   **评估指标**：原子稳定性（Atom Sta）、分子稳定性（Mol Sta）、有效性（Validity）、唯一性（Uniqueness）以及属性预测的平均绝对误差（MAE）。

### 4. 资源与算力
*   **硬件环境**：论文明确提到使用了 **Ubuntu 22.04** 系统和 **1 台 PRO6000 96GB GPU**。
*   **训练细节**：Batch Size 为 64，使用 Adam 优化器，学习率为 $1 \times 10^{-4}$。
*   **采样效率**：实验表明 EQUIMF 在达到相同稳定性阈值时，采样步数仅需基准模型的一半，实现了约 **2 倍的加速**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   主实验涵盖了两个主流分子数据集（QM9, DRUG）和三个合成数据集。
    *   进行了**条件生成实验**，验证了对 6 种分子属性的控制能力。
    *   **消融实验**：分别验证了“等变性”和“互调节机制”对模型性能的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了生成质量，还深入探讨了采样效率和物理一致性。通过 10,000 个样本的统计评估，结果具有统计学意义，实验设计客观公平。

### 6. 论文的主要结论与发现
*   **性能领先**：EQUIMF 在 QM9 和 DRUG 数据集上的分子稳定性、有效性等指标均优于现有的扩散和流匹配模型。
*   **效率提升**：通过 MeanFlow 的平均速度参数化，模型在极少数步数（Few-step）下即可生成高质量分子，显著优于需要大量 NFE（网络函数评估次数）的传统模型。
*   **物理一致性**：互调节机制显著提升了生成的原子价态正确性和几何构象的合理性。
*   **等变性优势**：实验证实 SE(3) 等变归纳偏置对于提高分子生成的稳定性至关重要。

### 7. 优点（亮点）
*   **理论创新**：将 MeanFlow 概念扩展到离散域，并提出了离散平均转移速率矩阵的参数化方案。
*   **架构优雅**：通过共享编码器实现了结构与几何的深度耦合，解决了长期存在的“解耦生成导致不一致”的问题。
*   **实用性强**：在保持高生成质量的同时，大幅降低了采样的时间成本，对药物发现等实际应用具有重要价值。

### 8. 不足与局限
*   **单步生成潜力未完全挖掘**：虽然 MeanFlow 理论上支持单步生成（One-step），但本文目前仍主要集中在少步（Few-step）生成以平衡质量，尚未完全实现极致的单步分子生成。
*   **计算复杂度**：虽然采样快，但由于引入了复杂的等变编码器和互调节机制，单次迭代的计算开销可能高于简单的非等变模型。
*   **应用范围限制**：目前主要验证了分子图生成，对于更大规模的蛋白质-配体复合物或超大规模材料结构的适用性尚待进一步验证。

（完）
