---
title: Constraint-Aware Corrective Memory for Language-Based Drug Discovery Agents
title_zh: 基于语言的药物发现智能体的约束感知纠错记忆
authors: "Maochen Sun, Youzhi Zhang, Gaofeng Meng"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09308v1"
tags: ["query:molrna"]
score: 8.0
evidence: 基于语言模型的药物发现智能体与分子集合约束
tldr: "针对基于语言的药物发现智能体在处理集合级约束（如多样性、结合质量）时存在的规划与评估不匹配问题，本文提出了CACM框架。该框架通过协议审计和落地诊断机制，结合多模态证据精确定位违规行为，并利用压缩的纠错记忆机制优化智能体状态。实验表明，CACM在目标达成率上比现有最先进基准提升了36.4%，证明了精确诊断和精简记忆对自主药物研发的重要性。"
selection_source: fresh_fetch
motivation: 现有的药物发现智能体在逐步规划时难以满足复杂的集合级协议约束，且冗长的历史记录导致状态噪声过多。
method: 提出CACM框架，通过协议审计定位违规，并利用分层压缩的纠错记忆机制为智能体提供精准的修复建议。
result: "在实验中，CACM相比当前最先进的基准系统将目标级别的成功率显著提升了36.4%。"
conclusion: 高效的自主药物研发不仅依赖强大的分子工具，更需要精确的故障诊断和经济的智能体状态管理。
---

## 摘要
大语言模型使得自主药物发现智能体变得日益可行，但在这种设定下，可靠的成功并非由单一动作或分子决定。它取决于最终返回的集合是否共同满足方案级要求，如集合大小、多样性、结合质量和可开发性。这产生了一个根本性的控制问题：智能体逐步进行规划，而任务的有效性是在整个候选集层面决定的。因此，现有的基于语言的药物发现系统往往依赖于冗长的原始历史记录和定义模糊的自我反思，导致故障定位不精确，且面向规划器的智能体状态噪声日益增加。我们提出了 CACM（约束感知纠错记忆），这是一个围绕精确的集合级诊断和简洁的记忆回写机制构建的基于语言的药物发现框架。CACM 引入了方案审计和落地诊断器，它们共同分析涵盖任务要求、口袋上下文和候选集证据的多模态证据，以定位方案违规行为，生成可操作的修复提示，并引导下一步动作转向最相关的修正。为了保持规划上下文的紧凑性，CACM 将记忆组织为静态、动态和纠错通道，并在回写前对其进行压缩，从而在保留持久任务信息的同时，仅暴露与决策最相关的故障。我们的实验结果表明，CACM 在目标级成功率上比最先进的基准提高了 36.4%。结果表明，可靠的基于语言的药物发现不仅受益于更强大的分子工具，还受益于更精确的诊断和更经济的智能体状态。

## Abstract
Large language models are making autonomous drug discovery agents increasingly feasible, but reliable success in this setting is not determined by any single action or molecule. It is determined by whether the final returned set jointly satisfies protocol-level requirements such as set size, diversity, binding quality, and developability. This creates a fundamental control problem: the agent plans step by step, while task validity is decided at the level of the whole candidate set. Existing language-based drug discovery systems therefore tend to rely on long raw history and under-specified self-reflection, making failure localization imprecise and planner-facing agent states increasingly noisy. We present CACM (Constraint-Aware Corrective Memory), a language-based drug discovery framework built around precise set-level diagnosis and a concise memory write-back mechanism. CACM introduces protocol auditing and a grounded diagnostician, which jointly analyze multimodal evidence spanning task requirements, pocket context, and candidate-set evidence to localize protocol violations, generate actionable remediation hints, and bias the next action toward the most relevant correction. To keep planning context compact, CACM organizes memory into static, dynamic, and corrective channels and compresses them before write-back, thereby preserving persistent task information while exposing only the most decision-relevant failures. Our experimental results show that CACM improves the target-level success rate by 36.4% over the state-of-the-art baseline. The results show that reliable language-based drug discovery benefits not only from more powerful molecular tools, but also from more precise diagnosis and more economical agent states.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CACM（Constraint-Aware Corrective Memory，约束感知纠错记忆）** 的框架，旨在提升基于大语言模型（LLM）的药物发现智能体在复杂约束下的可靠性。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心矛盾**：药物发现是一个迭代过程，其成功不仅取决于单个分子的质量，更取决于最终返回的**分子集合**是否共同满足协议要求（如集合大小、多样性、结合力、可开发性等）。
*   **现有瓶颈**：目前的 LLM 智能体（如 LIDDiA）在规划时是逐步进行的，而任务有效性是在集合层面评估的。这导致智能体难以精确定位失败原因，且往往依赖冗长的原始历史记录进行自我反思，导致上下文噪声大、规划效率低。
*   **研究目标**：通过引入精确的集合级诊断和精简的记忆机制，解决智能体在多约束分子设计中的控制失效问题。

### 2. 方法论：核心思想与关键技术
CACM 的核心思想是实现**精确诊断（Precision）**与**精简状态（Parsimony）**。
*   **协议审计（Protocol Audit）**：采用确定性的代码逻辑，对当前分子池进行硬性约束检查，计算各项指标（QED、SAS、多样性、对接得分等）与目标的残差。
*   **落地诊断器（Grounded Diagnoser）**：结合蛋白质口袋上下文、任务要求和审计残差，生成具体的修复提示（Repair Hint）和下一步动作偏好（Action Bias），而非模糊的自我反思。
*   **三通道结构化记忆**：
    *   **静态记忆（Static）**：存储不变的任务目标和口袋结构摘要。
    *   **动态描述记忆（Dynamic）**：存储演化的搜索状态、分子池摘要和近期动作窗口。
    *   **纠错记忆（Corrective）**：仅存储经过诊断的失败原因和修复建议。
*   **压缩与回写机制**：在每一轮迭代后，对三个通道的信息进行选择性压缩和格式化，确保面向规划器的上下文始终保持紧凑且与决策高度相关。

### 3. 实验设计
*   **数据集/场景**：使用了 LIDDiA 提出的包含 **30 个靶点** 的基准测试集，每个靶点包含特定的蛋白质口袋和自然语言描述的设计要求。
*   **基准方法（Benchmarks）**：
    *   **专用模型**：Pocket2Mol（基于结构的生成）、DiffSMOL（基于配体的生成）。
    *   **通用 LLM 智能体**：Claude、GPT-4o、o1-mini、o1。
    *   **直接对比对象**：LIDDiA（基于 DeepSeek-reasoner 重新实现的版本）。
*   **评估指标**：目标成功率（TSR）、分子质量指标（QED、SAS、Vina Score）、集合指标（多样性 DVS、集合大小 N）。

### 4. 资源与算力
*   **控制器模型**：主要使用 `deepseek-reasoner` 作为智能体决策核心。
*   **分子工具链**：Pocket2Mol（生成）、GraphGA（优化）、AutoDock Vina（对接评估）。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量。但提到了运行效率：CACM 平均每个靶点的运行时间约为 43.91 分钟，相比基准略有下降（因为迭代次数减少了）。

### 5. 实验数量与充分性
*   **实验规模**：针对 30 个靶点进行了完整测试，每靶点最多 10 次迭代。
*   **消融实验**：非常充分。分别验证了“集合级修复信号”、“结构化记忆”、“动态压缩”和“纠错选择”对结果的影响。
*   **客观性**：通过确定性的代码审计作为“地面真值（Ground Truth）”，避免了 LLM 自我评价的主观偏差。实验对比了从传统生成模型到最新推理模型（o1）的多种路径，具有较好的公平性。

### 6. 主要结论与发现
*   **性能提升**：CACM 将目标级成功率（TSR）从基准的 73.3% 提升至 **100%**，提升幅度达 36.4%。
*   **效率优化**：CACM 显著减少了达到成功所需的迭代次数（从 4.40 次降至 3.07 次），并使控制器端的 Token 消耗降低了 31.3%。
*   **控制逻辑**：实验证明，可靠的药物发现不仅需要强大的分子工具，更需要能够将集合级失败转化为精确动作指令的控制层。

### 7. 优点（亮点）
*   **控制与执行解耦**：将复杂的分子评估逻辑从 LLM 的模糊推理中剥离，交给确定性的审计模块，极大地提高了可靠性。
*   **记忆管理科学**：通过三通道记忆和压缩机制，解决了长程规划中上下文爆炸和信息漂移的问题。
*   **可解释性强**：诊断器输出的修复提示为智能体的每一步动作提供了明确的生物化学依据。

### 8. 不足与局限
*   **基准规模较小**：虽然 30 个靶点是该领域的标准基准，但对于验证通用药物发现能力来说，样本量仍显不足。
*   **工具链依赖**：系统的上限仍受限于底层分子工具（如 Pocket2Mol 和 Vina）的准确性，若底层工具产生错误引导，纠错记忆也难以挽回。
*   **实时性开销**：虽然总时长减少，但单次迭代中增加了额外的诊断调用，增加了单步推理的延迟。

（完）
