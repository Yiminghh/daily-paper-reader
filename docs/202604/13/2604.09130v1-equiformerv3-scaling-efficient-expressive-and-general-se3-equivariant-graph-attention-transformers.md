---
title: "EquiformerV3: Scaling Efficient, Expressive, and General SE(3)-Equivariant Graph Attention Transformers"
title_zh: EquiformerV3：扩展高效、强表达力且通用的 SE(3) 等变图注意力 Transformer
authors: "Yi-Lun Liao, Alexander J. Hoffman, Sabrina C. Shen, Alexandre Duval, Sam Walton Norwood, Tess Smidt"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09130v1"
tags: ["query:molrna"]
score: 9.0
evidence: 用于3D原子建模的SE(3)等变图注意力Transformer
tldr: EquiformerV3是针对3D原子建模设计的第三代SE(3)等变图注意力Transformer。它通过软件优化实现1.75倍加速，并引入等变合并层归一化、平滑半径截断注意力和SwiGLU-S²激活函数，显著提升了效率、表达能力和物理一致性。结合非平衡结构去噪辅助任务，该模型在OC20、OMat24等多个材料科学基准中达到SOTA性能，特别适用于需要能量守恒和高阶导数的模拟任务。
selection_source: fresh_fetch
motivation: 旨在解决大规模3D原子建模中SE(3)等变图神经网络在效率、表达能力和物理一致性（如势能面平滑度）方面的挑战。
method: 通过软件优化加速，并引入SwiGLU-S²激活函数、平滑截断注意力和改进的等变层归一化等架构优化。
result: 在OC20、OMat24和Matbench Discovery等主流材料科学数据集上取得了当前最先进的性能表现。
conclusion: EquiformerV3通过架构与算法的协同改进，成功实现了高效且物理一致的等变建模，为大规模原子模拟提供了强有力的工具。
---

## 摘要
随着 SE(3) 等变图神经网络作为 3D 原子建模的核心工具日益成熟，提高其效率、表达能力和物理一致性已成为大规模应用的核心挑战。在这项工作中，我们推出了 EquiformerV3，这是第三代 SE(3) 等变图注意力 Transformer，旨在提升效率、表达能力和通用性这三个维度。在 EquiformerV2 的基础上，我们实现了以下三个关键进展：首先，我们优化了软件实现，实现了 1.75 倍的加速；其次，我们对 EquiformerV2 进行了简单有效的改进，包括等变合并层归一化（equivariant merged layer normalization）、改进的前馈网络超参数以及具有平滑半径截断（smooth radius cutoff）的注意力机制；第三，我们提出了 SwiGLU-$S^2$ 激活函数，以引入多体相互作用从而获得更好的理论表达能力，并在降低 $S^2$ 网格采样复杂度的同时保持严格的等变性。SwiGLU-$S^2$ 激活函数和平滑截断注意力机制共同实现了对平滑变化的势能面（PES）的精确建模，使 EquiformerV3 能够推广到需要能量守恒模拟和势能面高阶导数的任务中。凭借这些改进，通过非平衡结构去噪（DeNS）辅助任务训练的 EquiformerV3 在 OC20、OMat24 和 Matbench Discovery 数据集上取得了当前最先进的结果。

## Abstract
As $SE(3)$-equivariant graph neural networks mature as a core tool for 3D atomistic modeling, improving their efficiency, expressivity, and physical consistency has become a central challenge for large-scale applications. In this work, we introduce EquiformerV3, the third generation of the $SE(3)$-equivariant graph attention Transformer, designed to advance all three dimensions: efficiency, expressivity, and generality. Building on EquiformerV2, we have the following three key advances. First, we optimize the software implementation, achieving $1.75\times$ speedup. Second, we introduce simple and effective modifications to EquiformerV2, including equivariant merged layer normalization, improved feedforward network hyper-parameters, and attention with smooth radius cutoff. Third, we propose SwiGLU-$S^2$ activations to incorporate many-body interactions for better theoretical expressivity and to preserve strict equivariance while reducing the complexity of sampling $S^2$ grids. Together, SwiGLU-$S^2$ activations and smooth-cutoff attention enable accurate modeling of smoothly varying potential energy surfaces (PES), generalizing EquiformerV3 to tasks requiring energy-conserving simulations and higher-order derivatives of PES. With these improvements, EquiformerV3 trained with the auxiliary task of denoising non-equilibrium structures (DeNS) achieves state-of-the-art results on OC20, OMat24, and Matbench Discovery.

---

## 论文详细总结（自动生成）

以下是对论文《EquiformerV3: Scaling Efficient, Expressive, and General SE(3)-Equivariant Graph Attention Transformers》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：SE(3) 等变图神经网络（GNN）已成为 3D 原子建模（如分子动力学、催化剂设计）的核心工具。然而，在大规模应用中，如何在保证**计算效率**的同时，提升模型的**表达能力**（Expressivity）和**物理一致性**（如势能面的平滑度和能量守恒）仍是主要挑战。
*   **核心问题**：前代模型（如 EquiformerV2）虽然在单点预测上表现优异，但在需要高阶导数的模拟任务（如热导率计算）中表现欠佳，且计算开销随表示阶数（$L_{max}$）增加而剧增。
*   **研究动机**：开发第三代 Equiformer，通过架构优化和新型激活函数，实现效率、表达力和通用性的全面提升。

### 2. 方法论
EquiformerV3 在 V2 的基础上引入了三大核心改进：
*   **软件实现优化**：
    *   融合冗余操作：将置换矩阵融合进旋转矩阵中，消除 eSCN 卷积中的冗余计算。
    *   启用编译：修复动态形状问题，支持 `torch.compile()`，实现 **1.75 倍**的训练加速。
*   **架构细节改进**：
    *   **等变合并层归一化（Equivariant Merged LN）**：不同于 V2 对不同阶数独立归一化，V3 计算所有阶数的合并均方根（RMS）并共享，保留了不同阶数特征间的相对重要性。
    *   **平滑半径截断注意力**：在注意力机制的 Softmax 中引入包络函数（Envelope Function），确保原子进出截断半径时能量预测的连续性，从而支持能量守恒模拟。
*   **SwiGLU-$S^2$ 激活函数**：
    *   **核心思想**：将特征投影到单位球 $S^2$ 网格上，利用标量特征进行门控（Strict Equivariance），并在网格空间进行元素级乘法。
    *   **多体相互作用**：网格间的乘法等效于特征的自张量积，理论上引入了更高阶的体序（Body-order），显著增强了局部结构的区分能力。
    *   **效率提升**：相比传统的 $S^2$ 激活，SwiGLU-$S^2$ 减少了高频分量的引入，在保持严格等变性的前提下，可减少约 50% 的网格采样点。

### 3. 实验设计
*   **数据集**：
    *   **OC20 (Open Catalyst 2020)**：S2EF 任务，评估能量和力的预测精度。
    *   **OMat24 (Open Materials 2024)**：包含 1.1 亿个非平衡晶体结构，评估能量、力和应力。
    *   **Matbench Discovery**：评估晶体稳定性预测（F1 分数）和热导率（$\kappa_{SRME}$，涉及 PES 高阶导数）。
*   **对比方法**：包括 EquiformerV2、eSEN、UMA、MACE、NequIP、PET 等当前最先进的等变模型。
*   **辅助任务**：结合了非平衡结构去噪（DeNS）任务进行预训练。

### 4. 资源与算力
*   **硬件**：主要使用 **NVIDIA H100 GPU**。
*   **训练时长**：
    *   **OC20 (2M)**：消融实验模型约需 150-170 GPU 小时。
    *   **OMat24**：$L_{max}=6$ 的模型预训练加微调共需约 **9200 GPU 小时**。
    *   **Matbench Discovery**：非合规模型（基于 OMat24 预训练）的训练总耗时约 **5700 GPU 小时**，相比竞争对手 UMA（>12.9 万小时）大幅缩减。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从百万级（OC20-2M）到亿级（OMat24）的数据规模，实验设计非常充分。
*   **消融实验**：在 OC20 上对软件优化、归一化方式、FFN 超参数、平滑截断和 SwiGLU-$S^2$ 进行了逐项验证。
*   **理论验证**：专门设计了“体序实验”（Body-order experiment），证明了 SwiGLU-$S^2$ 在单层消息传递下区分复杂几何结构的能力优于 Gate 和普通 $S^2$ 激活。
*   **客观性**：使用了公开的 Leaderboard 评价体系（如 Matbench Discovery 的 CPS 分数），对比基准涵盖了最新发表的同类模型，评价较为公正。

### 6. 主要结论与发现
*   **性能领先**：在 OC20、OMat24 和 Matbench Discovery 上均取得了 **SOTA（当前最佳）** 结果。
*   **效率飞跃**：在 OC20 上实现 5.9 倍的训练效率提升；在 Matbench 上以更小的参数量（30M-50M）击败了参数量大得多的模型（如 700M 的 UMA-L）。
*   **物理一致性**：通过平滑截断和严格等变激活，EquiformerV3 在热导率预测任务中表现优异，证明其能准确捕捉势能面的高阶导数。
*   **首个突破**：在 Matbench Discovery 综合性能评分（CPS）上首次突破 **0.9** 大关。

### 7. 优点
*   **兼顾理论与工程**：既有对多体相互作用的理论探讨，又有针对 CUDA 实现的底层软件优化。
*   **通用性强**：模型不仅在催化剂（OC20）上表现好，在通用材料（OMat24）和复杂物理属性预测（Matbench）上也展现了极强的泛化能力。
*   **资源友好**：通过算法优化显著降低了等变 Transformer 的训练门槛，使其在大规模数据集上的迭代更具可行性。

### 8. 不足与局限
*   **数据瓶颈**：作者发现当模型增大到一定程度后，在 Matbench 上的收益递减，暗示当前的训练数据质量或多样性（如缺乏声子数据、双原子数据）可能成为新的瓶颈。
*   **实现复杂度**：SE(3) 等变操作及 $S^2$ 网格投影的数学实现较为复杂，对普通开发者有一定的门槛。
*   **应用限制**：虽然在材料和催化领域表现优异，但在生物大分子（如蛋白质）等更长程相互作用系统的表现尚未在本文中验证。

（完）
