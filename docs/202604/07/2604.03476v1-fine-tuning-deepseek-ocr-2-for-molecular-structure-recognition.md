---
title: Fine-tuning DeepSeek-OCR-2 for Molecular Structure Recognition
title_zh: 微调 DeepSeek-OCR-2 用于分子结构识别
authors: "Haocheng Tang, Xingyu Dang, Junmei Wang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03476v1"
tags: ["query:molrna"]
score: 8.0
evidence: 微调视觉语言模型用于分子结构识别
tldr: 本研究针对化学结构识别（OCSR）任务，通过微调DeepSeek-OCR-2模型实现了从分子图像到SMILES字符串的端到端转换。为解决训练不稳定性，提出了一种结合LoRA与选择性全参数微调的两阶段渐进式策略，并在大规模合成及真实专利数据集上进行训练。结果显示，所开发的MolSeek-OCR模型在精确匹配率上可媲美顶尖的图像到序列模型，为利用通用视觉语言模型处理专业科学领域任务提供了重要参考。
selection_source: fresh_fetch
motivation: 旨在克服视觉语言模型在分子结构识别任务中直接进行全参数微调时存在的训练不稳定及性能受限问题。
method: 采用两阶段渐进式微调策略，即先通过LoRA进行初步适配，再利用分层学习率进行选择性的全参数微调。
result: MolSeek-OCR在分子识别精度上达到了主流图像到序列模型的领先水平，但在结构化图建模方面仍有提升空间。
conclusion: 研究发现强化学习等后训练手段对提升SMILES序列的精确匹配效果有限，强调了针对性微调策略在专业OCR任务中的重要性。
---

## 摘要
光学化学结构识别 (OCSR) 对于将印刷文献中的二维分子图转换为机器可读格式至关重要。虽然视觉语言模型在端到端 OCR 任务中表现出潜力，但将其直接应用于 OCSR 仍具挑战性，且直接的全参数监督微调往往会失败。在这项工作中，我们将 DeepSeek-OCR-2 适配于分子光学识别，将该任务建模为图像条件下的 SMILES 生成。为了克服训练不稳定性，我们提出了一种两阶段渐进式监督微调策略：从参数高效的 LoRA 开始，逐步过渡到采用分层学习率的选择性全参数微调。我们在结合了来自 PubChem 的合成渲染图和来自 USPTO-MOL 的真实专利图像的大规模语料库上训练模型，以提高覆盖范围和鲁棒性。我们微调后的模型 MolSeek-OCR 展示了具有竞争力的能力，实现了与性能最佳的图像到序列模型相当的精确匹配准确率。然而，它仍逊于最先进的图像到图模型。此外，我们探索了强化学习风格的后训练和基于数据清洗的精炼，发现它们未能提高精确 SMILES 匹配所需的严格序列级保真度。

## Abstract
Optical Chemical Structure Recognition (OCSR) is critical for converting 2D molecular diagrams from printed literature into machine-readable formats. While Vision-Language Models have shown promise in end-to-end OCR tasks, their direct application to OCSR remains challenging, and direct full-parameter supervised fine-tuning often fails. In this work, we adapt DeepSeek-OCR-2 for molecular optical recognition by formulating the task as image-conditioned SMILES generation. To overcome training instabilities, we propose a two-stage progressive supervised fine-tuning strategy: starting with parameter-efficient LoRA and transitioning to selective full-parameter fine-tuning with split learning rates. We train our model on a large-scale corpus combining synthetic renderings from PubChem and realistic patent images from USPTO-MOL to improve coverage and robustness. Our fine-tuned model, MolSeek-OCR, demonstrates competitive capabilities, achieving exact matching accuracies comparable to the best-performing image-to-sequence model. However, it remains inferior to state-of-the-art image-to-graph modelS. Furthermore, we explore reinforcement-style post-training and data-curation-based refinement, finding that they fail to improve the strict sequence-level fidelity required for exact SMILES matching.

---

## 论文详细总结（自动生成）

以下是对论文《Fine-tuning DeepSeek-OCR-2 for Molecular Structure Recognition》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：光学化学结构识别（OCSR）是将印刷文献中的二维分子图转换为机器可读格式（如 SMILES）的关键技术。虽然视觉语言模型（VLM）在通用 OCR 任务中表现出色，但直接将其应用于高度专业化的化学结构识别时，面临训练不稳定、全参数微调易失败等挑战。
*   **核心问题**：如何有效地微调通用的文档理解大模型（如 DeepSeek-OCR-2），使其在保持鲁棒性的同时，能够精确解析复杂的分子结构图像。

### 2. 方法论
论文提出了一种**两阶段渐进式监督微调（SFT）策略**，将任务建模为图像条件下的 SMILES 序列生成：
*   **核心思想**：通过从参数高效微调到选择性全参数微调的过渡，克服 VLM 在处理专业结构数据时的优化难题。
*   **关键技术细节**：
    *   **第一阶段（LoRA SFT）**：在 DeepSeek-OCR-2 的注意力层、前馈网络及跨模态投影接口上应用 LoRA。此阶段旨在初步建立视觉特征与化学字符串之间的对齐。
    *   **第二阶段（渐进式全参数微调）**：冻结底层的视觉 Tokenizer 和输入嵌入层，仅优化高层 LM 视觉编码器、投影接口和解码器。
    *   **分层学习率**：为视觉分支设置较低的学习率（$5 \times 10^{-6}$），为语言生成分支设置较高的学习率（$2 \times 10^{-5}$），以平衡感知与生成能力的更新。
    *   **多尺度视觉流水线**：保留原模型的全局视图与局部裁剪机制，以捕捉分子的整体拓扑和局部化学键细节。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：结合了来自 PubChem 的合成数据（包括 MolScribe 风格和 ChemDraw 风格渲染）以及来自 USPTO-MOL 的真实专利图像，规模从 192k 扩展到 800k。
    *   **测试集（Benchmark）**：涵盖合成集（Indigo, ChemDraw）、真实集（CLEF, UOB, USPTO, Staker, ACS）以及针对外观变化和质量退化的扰动集。
*   **对比方法**：
    *   **规则模型**：MolVec, OSRA。
    *   **图像到序列模型**：Img2Mol, DECIMER（该领域此前的领先模型）, SwinOCSR。
    *   **图像到图模型**：MolScribe（目前的最强基准）, Image2Graph 等。

### 4. 资源与算力
*   **训练细节**：使用 AdamW 优化器。LoRA 阶段训练 3000 步，全参数阶段训练 2500 步。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号（如 A100 或 H100）及具体数量，仅提到 Batch Size 为每设备 4，梯度累积为 8。

### 5. 实验数量与充分性
*   **实验规模**：论文在 11 个不同的基准数据集（含 4 个扰动集）上进行了测试，涵盖了从实验室合成图到真实专利扫描件的多种场景。
*   **充分性与客观性**：实验设计较为全面，不仅对比了同类“图像到序列”模型，还跨类别对比了“图像到图”模型。此外，作者还客观地记录并分析了强化学习（GSPO）和数据精炼（ReFT）在提升精确匹配率方面的**失败尝试**，这为后续研究提供了重要的负面参考。

### 6. 主要结论与发现
*   **性能表现**：微调后的 **MolSeek-OCR** 在精确匹配准确率上显著超越了基础模型（零开销下几乎为 0%），并达到了与 DECIMER 相当的水平。
*   **架构局限**：尽管 VLM 表现强劲，但在 OCSR 任务上仍逊于以 MolScribe 为代表的“图像到图”模型，说明显式预测原子和键的几何布局比纯文本序列生成更具优势。
*   **后训练失效**：强化学习（GSPO）虽然能微弱提升图层面的准确性，但往往会破坏 SMILES 字符串的严格序列保真度，导致精确匹配率下降。

### 7. 优点
*   **策略创新**：提出的两阶段微调策略有效解决了通用 VLM 在专业领域微调时的不稳定性问题。
*   **鲁棒性强**：通过混合合成与真实数据，模型在面对模糊、噪声等扰动图像时表现出较好的泛化能力。
*   **开源贡献**：提供了完整的训练数据集、代码和模型参数。

### 8. 不足与局限
*   **序列敏感性**：SMILES 是一种高度敏感的表示法，序列中一个字符的错误会导致整个分子识别失败，而 VLM 的自回归生成机制难以完全避免此类随机错误。
*   **性能瓶颈**：相比于专门为化学结构设计的图生成模型，通用 VLM 架构在处理复杂手性、重叠化学键等精细结构时仍显乏力。
*   **算力信息缺失**：未披露具体算力消耗，不便于其他研究者评估复现成本。

（完）
