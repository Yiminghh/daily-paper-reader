---
title: A Generative Foundation Model for Multimodal Histopathology
title_zh: 一种用于多模态组织病理学的生成式基础模型
authors: "Jinxi Xiang, Mingjie Li, Siyu Hou, Yijiang Chen, Xiangde Luo, Yuanfeng Ji, Xiang Zhou, Ehsan Adeli, Akshay Chaudhari, Curtis P. Langlotz, Kilian M. Pohl, Ruijiang Li"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03635v1"
tags: ["query:molrna"]
score: 9.0
evidence: 分子RNA谱的生成式基础模型
tldr: 针对病理诊断中多模态数据缺失及现有模型泛化性差的问题，本文提出MuPD生成式基础模型。该模型基于扩散Transformer架构，在包含1亿张病理切片、千万级RNA及文本对的大规模数据集上预训练，实现了组织学、分子和临床数据的统一表征。MuPD在文本/RNA驱动的图像合成、虚拟染色等任务中表现卓越，显著提升了生成质量与下游分类精度，为多模态计算病理学提供了高效、通用的计算框架。
selection_source: fresh_fetch
motivation: 现有的病理学计算方法通常依赖于针对特定任务训练的单一模态对模型，难以应对临床实践中多模态数据缺失且泛化能力受限的挑战。
method: "开发了名为MuPD的生成式基础模型，通过具有解耦交叉注意力机制的扩散Transformer，将H&E染色图像、RNA谱图和临床文本嵌入统一的共享潜空间。"
result: "MuPD在跨模态合成任务中将FID分数降低了23%-50%，并在虚拟染色和少样本分类增强方面显著优于现有的专用模型。"
conclusion: 研究表明，在异构病理模态上预训练的统一生成式基础模型能够超越专用模型，为多模态病理学研究提供了一个可扩展的通用框架。
---

## 摘要
复杂疾病的准确诊断和治疗需要整合组织学、分子和临床数据，但在实践中，由于组织稀缺、检测成本和工作流程限制，这些模态往往是不完整的。现有的计算方法试图从可用数据中推补缺失的模态，但依赖于在狭窄的单一源-目标对上训练的特定任务模型，限制了其泛化能力。在此，我们介绍了 MuPD（多模态病理扩散模型），这是一种生成式基础模型，它通过具有解耦跨模态注意力的扩散 Transformer，将苏木精-伊红（H&E）染色的组织学、分子 RNA 谱和临床文本嵌入到一个共享的潜空间中。MuPD 在涵盖 34 个人体器官的 1 亿个组织学图像块、160 万个文本-组织学对和 1080 万个 RNA-组织学对上进行了预训练，支持多种跨模态合成任务，且只需极少或无需特定任务的微调。对于文本条件和图像到图像的生成，MuPD 合成了在组织学上真实的组织结构，相对于领域特定模型，将 Fréchet Inception Distance (FID) 分数降低了 50%，并通过合成数据增强将少样本分类准确率提高了多达 47%。对于 RNA 条件的组织学生成，MuPD 与次优方法相比将 FID 降低了 23%，同时保留了五种癌症类型的细胞类型分布。作为一种虚拟染色工具，MuPD 将 H&E 图像转换为免疫组化和多重免疫荧光图像，与现有方法相比，平均标志物相关性提高了 37%。这些结果表明，在异构病理模态上预训练的单一统一生成模型可以显著优于专用替代方案，为多模态组织病理学提供了一个可扩展的计算框架。

## Abstract
Accurate diagnosis and treatment of complex diseases require integrating histological, molecular, and clinical data, yet in practice these modalities are often incomplete owing to tissue scarcity, assay cost, and workflow constraints. Existing computational approaches attempt to impute missing modalities from available data but rely on task-specific models trained on narrow, single source-target pairs, limiting their generalizability. Here we introduce MuPD (Multimodal Pathology Diffusion), a generative foundation model that embeds hematoxylin and eosin (H&E)-stained histology, molecular RNA profiles, and clinical text into a shared latent space through a diffusion transformer with decoupled cross-modal attention. Pretrained on 100 million histology image patches, 1.6 million text-histology pairs, and 10.8 million RNA-histology pairs spanning 34 human organs, MuPD supports diverse cross-modal synthesis tasks with minimal or no task-specific fine-tuning. For text-conditioned and image-to-image generation, MuPD synthesizes histologically faithful tissue architectures, reducing Fréchet inception distance (FID) scores by 50% relative to domain-specific models and improving few-shot classification accuracy by up to 47% through synthetic data augmentation. For RNA-conditioned histology generation, MuPD reduces FID by 23% compared with the next-best method while preserving cell-type distributions across five cancer types. As a virtual stainer, MuPD translates H&E images to immunohistochemistry and multiplex immunofluorescence, improving average marker correlation by 37% over existing approaches. These results demonstrate that a single, unified generative model pretrained across heterogeneous pathology modalities can substantially outperform specialized alternatives, providing a scalable computational framework for multimodal histopathology.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MUPAD**（Multimodal Pathology Diffusion，多模态病理扩散模型）的生成式基础模型，旨在解决计算病理学中多模态数据缺失和模型碎片化的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代病理诊断依赖 H&E 染色、免疫组化（IHC）、多重免疫荧光（mIF）及分子谱（RNA）等多模态数据。然而，在临床实践中，由于组织样本稀缺、检测成本高昂和工作流程限制，这些数据往往不完整。
*   **研究动机**：现有的计算方法（如虚拟染色、跨模态预测）大多是针对特定任务（如 H&E 转 IHC）在小规模配对数据集上训练的“孤岛式”模型，缺乏泛化能力，且无法利用不同模态间的共享生物学先验。

### 2. 方法论
*   **核心思想**：将 H&E 组织学图像作为“桥梁模态”，通过一个统一的生成式基础模型，将图像、分子 RNA 谱和临床文本嵌入到共享的潜空间中，实现任意模态间的条件生成。
*   **关键技术细节**：
    *   **架构**：基于扩散 Transformer（DiT）和可扩展插值 Transformer（SiT）构建。
    *   **解耦交叉注意力机制（DCA）**：这是模型的核心创新。为了处理异构输入（文本、RNA、图像），模型为每种模态设计了独立的并行注意力流，避免了不同语义特征在共享投影空间中的相互干扰。
    *   **特征对齐与蒸馏**：利用预训练的病理编码器（如 Virchow2）作为教师模型，通过 CNN 投影器进行空间特征对齐，并引入辅助的 CLS 标记去噪损失以确保全局语义一致性。
    *   **条件输入处理**：使用 MUSK 模型编码文本和图像，将 bulk RNA-seq 数据压缩为 331 个生物通路评分，以实现生物学上的可解释性。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用了来自 TCGA、GTEx、PAIP 和 PLCO 的 1 亿个 H&E 图像块，160 万个文本-图像对，以及 1080 万个 RNA-图像对，涵盖 34 个人体器官。
    *   **下游评估**：包括 MOSAIC（空间转录组）、ORION-CRC（多重免疫荧光）、IHC4BC 和 HER2Match（免疫组化）、TCGA 肺/脑队列（冰冻切片转石蜡切片）等。
*   **Benchmark 与对比方法**：
    *   **通用生成模型**：Stable Diffusion 3.5, FLUX.2。
    *   **病理专用模型**：PathLDM, PixCell, GeneFlow（RNA 驱动）, AI-FFPE（切片转换）, CUT/CycleGAN（虚拟染色）, GigaTIME/HistoPlexer（mIF 预测）。
*   **评估指标**：FID（分布保真度）、KID、图像相似度、文本-图像对齐度、Pearson 相关系数（PCC）、临床指标 AUC（如 HER2 状态预测）。

### 4. 资源与算力
*   **算力配置**：模型在 **8 台 NVIDIA H100 GPU** 上进行训练。
*   **训练时长**：预训练步数高达 **500,000 步**，使用 AdamW 优化器和 bf16 精度。推理阶段采用 250 步 SDE 采样。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，涵盖了 7 大类任务：图像到图像生成、文本到图像生成、少样本分类数据增强、视觉-语言检索增强、空间转录组转 H&E、冰冻切片转石蜡切片（FF-to-FFPE）、H&E 转 IHC/mIF。
*   **消融实验**：专门对比了“解耦交叉注意力”与“共享交叉注意力”的效果，以及不同对齐损失（REPA vs. MUPAD 空间对齐）对收敛速度和质量的影响。
*   **评价**：实验设计非常充分且客观，不仅对比了最先进的生成模型，还通过下游临床任务（如生物标志物预测）验证了生成图像的生物学真实性。

### 6. 主要结论与发现
*   **统一性优势**：单一的统一模型在几乎所有任务上都显著优于专门针对特定任务训练的模型。
*   **性能提升**：在文本/图像生成中，FID 降低了约 50%；在虚拟染色任务中，标志物相关性提升了 37%。
*   **数据增强价值**：MUPAD 生成的合成数据在少样本分类任务中将准确率最高提升了 47%，证明了其捕获了真实的形态学先验。
*   **生物学一致性**：模型在 RNA 驱动的生成中能准确保留细胞类型分布，在虚拟 IHC 中能达到极高的临床诊断一致性（Ki67 预测 AUC 达 0.977）。

### 7. 优点
*   **多模态融合能力强**：通过 DCA 机制成功解决了文本、数值（RNA）和图像特征的融合难题。
*   **零样本/少样本迁移**：预训练规模巨大，使得模型在未见过的空间转录组数据上表现出极强的零样本迁移能力。
*   **临床实用性高**：不仅追求视觉上的“像”，更通过临床金标准验证了生成结果的诊断价值。

### 8. 不足与局限
*   **随机性风险**：作为扩散模型，生成过程具有随机性，可能在分布外数据上产生解剖学上不合理的伪影（Hallucinations）。
*   **验证局限**：虽然在多个数据集上表现优异，但临床部署仍需在前瞻性队列中进行更严格的验证。
*   **计算成本**：推理阶段的 SDE 采样（250 步）相对较慢，可能限制其在实时诊断场景中的应用。

（完）
