---
title: Toward a universal foundation model for graph-structured data
title_zh: 迈向图结构数据的通用基础模型
authors: "Sakib Mostafa, Lei Xing, Md. Tauhidul Islam"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06391v1"
tags: ["query:molrna"]
score: 9.0
evidence: 生物医学图结构数据的通用基础模型
tldr: 本研究针对生物医学领域图数据缺乏通用基础模型的问题，提出了一种能够学习可迁移结构表示的图基础模型。该模型通过将度统计、中心性、社区结构等特征无关的图属性编码为结构提示，并结合消息传递机制，实现了在异构图上的预训练。实验证明，该模型在零样本和少样本任务中表现优异，显著提升了图分析的泛化能力和预测精度，为图结构数据提供了通用的表示空间。
selection_source: fresh_fetch
motivation: 现有的图神经网络通常受限于特定数据集的节点特征和拓扑结构，导致其在不同领域和数据集之间缺乏泛化能力。
method: 通过提取度统计、中心性和扩散特征等特征无关的图属性作为结构提示，并结合消息传递骨干网络构建预训练模型。
result: "该模型在多个基准测试中表现优于强监督基线，在SagePPI任务上实现了95.5%的ROC-AUC，比现有最佳模型提升了21.8%。"
conclusion: 该研究成功开发出一种适用于生物医学和网络科学的可复用通用图基础模型，有效解决了图数据的跨域迁移难题。
---

## 摘要
图是生物医学研究中的核心表示形式，涵盖了分子相互作用网络、基因调节回路、细胞间通信图谱以及知识图谱。尽管图具有重要性，但目前尚无能够与改变语言和视觉领域的模型相媲美的、广泛可重用的图分析基础模型。现有的图神经网络通常在单一数据集上进行训练，并学习仅针对该图的节点特征、拓扑结构和标签空间的特定表示，从而限制了它们跨领域迁移的能力。这种泛化能力的缺失在生物学和医学中尤为突出，因为这些领域的网络在不同队列、实验分析和机构之间存在显著差异。在此，我们介绍了一种图基础模型，旨在学习不依赖于特定节点身份或特征方案的可迁移结构表示。我们的方法利用了与特征无关的图属性，包括度统计、中心性度量、社区结构指标和基于扩散的特征，并将它们编码为结构提示（structural prompts）。这些提示与消息传递骨干网络集成，将不同的图嵌入到共享的表示空间中。该模型在异构图上进行一次预训练，随后以极小的适配成本重用于未见的数据集。在多个基准测试中，我们的预训练模型达到或超过了强大的有监督基准模型，同时在留出图上展示了卓越的零样本（zero-shot）和少样本（few-shot）泛化能力。在 SagePPI 基准测试中，对预训练骨干网络进行有监督微调达到了 95.5% 的平均 ROC-AUC，比最佳的有监督消息传递基准模型提高了 21.8%。因此，所提出的技术为生物医学和网络科学应用中图结构数据的可重用、基础规模模型提供了一种独特的方法。

## Abstract
Graphs are a central representation in biomedical research, capturing molecular interaction networks, gene regulatory circuits, cell--cell communication maps, and knowledge graphs. Despite their importance, currently there is not a broadly reusable foundation model available for graph analysis comparable to those that have transformed language and vision. Existing graph neural networks are typically trained on a single dataset and learn representations specific only to that graph's node features, topology, and label space, limiting their ability to transfer across domains. This lack of generalization is particularly problematic in biology and medicine, where networks vary substantially across cohorts, assays, and institutions. Here we introduce a graph foundation model designed to learn transferable structural representations that are not specific to specific node identities or feature schemes. Our approach leverages feature-agnostic graph properties, including degree statistics, centrality measures, community structure indicators, and diffusion-based signatures, and encodes them as structural prompts. These prompts are integrated with a message-passing backbone to embed diverse graphs into a shared representation space. The model is pretrained once on heterogeneous graphs and subsequently reused on unseen datasets with minimal adaptation. Across multiple benchmarks, our pretrained model matches or exceeds strong supervised baselines while demonstrating superior zero-shot and few-shot generalization on held-out graphs. On the SagePPI benchmark, supervised fine-tuning of the pretrained backbone achieves a mean ROC-AUC of 95.5%, a gain of 21.8% over the best supervised message-passing baseline. The proposed technique thus provides a unique approach toward reusable, foundation-scale models for graph-structured data in biomedical and network science applications.

---

## 论文详细总结（自动生成）

这是一份关于论文《Toward a universal foundation model for graph-structured data》（迈向图结构数据的通用基础模型）的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管图神经网络（GNN）在生物医学等领域应用广泛，但目前缺乏像自然语言处理（NLP）或计算机视觉（CV）那样通用的“基础模型”。
*   **研究动机**：
    *   **数据集特异性**：现有GNN通常针对特定图的节点特征、拓扑和标签空间进行训练，难以跨领域迁移。
    *   **生物医学挑战**：在生物学中，不同队列、实验和机构产生的网络结构差异巨大，模型泛化能力极差。
    *   **输入空间不统一**：图数据不像图像或文本有标准格式，节点特征（如分子指纹、连续属性）和边缘含义在不同任务中完全不同。

### 2. 方法论：核心思想与技术细节
该模型（GFM）的核心思想是**利用“特征无关”的图拓扑属性作为通用接口**，将异构图映射到共享的表示空间。
*   **结构提示（Structural Prompts）**：
    *   **局部上下文**：提取节点度、聚类系数、k-core数、自我网络（ego-network）统计量和PageRank。
    *   **全局上下文**：利用标签传播（Label Propagation）和SCoDA两种社区检测算法，提取社区身份、规模和密度。
    *   **文本化编码**：将上述拓扑指标组合成自然语言描述，使用小型语言模型（MiniLM）将其转化为384维的上下文嵌入。
*   **模型架构**：
    *   **骨干网络**：采用两层GraphSAGE作为消息传递机制。
    *   **图特征嵌入（GFE）**：将原始节点特征与结构提示嵌入拼接，通过一个可学习的适配器（Adapter）投影到高维空间。
*   **预训练策略**：
    *   **多图对比学习**：在9个非生物医学图（如论文引用网络、电商共同购买网络）上进行预训练。
    *   **损失函数**：使用InfoNCE对比损失，通过个性化PageRank（PPR）识别结构相关的正样本对，对齐“文本流”和“图流”的表示。
*   **下游适配**：在未见过的图上，冻结骨干网络，仅通过无监督的对比学习微调轻量级适配器。

### 3. 实验设计
*   **数据集与场景**：
    1.  **SagePPI**：人类蛋白质相互作用网络（121个GO生物过程标签）。
    2.  **ogbn-proteins**：大规模STRING网络，采用物种分层划分（测试集物种在训练中未见）。
    3.  **StringGO**：使用氨基酸组成作为节点特征，测试跨本体（分子功能、细胞组分等）泛化。
    4.  **Fold-PPI**：144个组织特异性网络，测试完全未见的蛋白质折叠类别的泛化。
*   **对比基准（Baselines）**：GCN、GIN、GAT、GraphSAGE（均为在目标任务上从头训练的有监督模型）。
*   **评估模式**：零样本（Zero-shot）、少样本（Few-shot，K=1, 5, 10, 20）以及有监督微调。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的GPU型号和数量。
*   **训练细节**：
    *   预训练：250个epoch，每个epoch 128步，Batch Size为1024。
    *   优化器：Adam，学习率为 $10^{-5}$。
    *   应用了混合精度训练（Mixed-precision training）以提高效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了4个大型基准测试，涉及数万个节点和数千万条边。
*   **充分性**：
    *   **跨领域验证**：模型在非生物图上预训练，在生物图上测试，验证了极强的跨域迁移能力。
    *   **多维度分析**：除了分类精度，还进行了GO层次深度分析、多功能性相关性分析、噪声鲁棒性测试以及t-SNE可视化。
*   **公平性**：所有基准模型均在相同的训练/验证/测试划分下运行，且GFM在零样本模式下与全监督基准对比，体现了其优越性。

### 6. 主要结论与发现
*   **拓扑是通用语言**：即使在非生物图上训练，模型也能捕捉到适用于生物网络的通用组织原则（如枢纽蛋白、社区结构）。
*   **性能卓越**：在SagePPI上，GFM微调后的ROC-AUC达到95.5%，比最强基准提升21.8%。在零样本模式下，GFM仅凭1个标注样本的效果即可比拟基准模型20个样本的效果。
*   **生物学一致性**：GFM的嵌入空间能够自动恢复亚细胞定位（线粒体、细胞核等）和功能模块，且对节点特征噪声具有极强的鲁棒性。
*   **泛化深度**：在ogbn-proteins中，GFM在更具体（更深层）的GO术语上表现更好，说明其捕捉长程结构信号的能力强于传统GNN。

### 7. 优点与亮点
*   **特征无关性**：通过结构提示解决了图数据节点特征不统一的痛点，实现了真正的“即插即用”。
*   **零样本能力**：展示了在完全没有目标任务标签的情况下，依然能通过拓扑结构提供高质量表示的能力。
*   **生物学解释性**：实验证明了模型学习到的拓扑特征与生物学功能（如多功能性、折叠类别）高度相关。

### 8. 不足与局限
*   **拓扑依赖性**：如果目标图的边缘非常稀疏或存在大量噪声，基于社区检测和扩散统计的结构提示可能会失效。
*   **转导式限制**：目前的适配过程需要访问目标图的完整拓扑结构，对于动态流式图或隐私受限的图数据，其归纳推理（Inductive）能力尚待加强。
*   **微调稳定性**：在某些极其复杂且标签极度不平衡的本体（如StringGO中的BP）上，全监督微调的效果可能不如零样本稳定。
*   **算力细节缺失**：未提供具体的硬件资源消耗，难以评估普通研究机构复现预训练过程的成本。

（完）
